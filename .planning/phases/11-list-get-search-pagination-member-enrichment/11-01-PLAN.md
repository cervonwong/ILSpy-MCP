---
phase: 11-list-get-search-pagination-member-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Transport/Mcp/Tools/ListAssemblyTypesTool.cs
  - Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs
  - Transport/Mcp/Tools/SearchMembersByNameTool.cs
  - Application/UseCases/ListAssemblyTypesUseCase.cs
  - Application/UseCases/ListEmbeddedResourcesUseCase.cs
  - Application/UseCases/SearchMembersByNameUseCase.cs
  - Tests/Tools/ListAssemblyTypesToolTests.cs
  - Tests/Tools/ListEmbeddedResourcesToolTests.cs
  - Tests/Tools/SearchMembersByNameToolTests.cs
autonomous: true
requirements:
  - PAGE-03
  - PAGE-05

must_haves:
  truths:
    - "list_assembly_types accepts (maxResults, offset) and returns [pagination:{total,returned,offset,truncated,nextOffset}] footer"
    - "list_embedded_resources accepts (maxResults, offset) and returns [pagination:{...}] footer"
    - "search_members_by_name accepts (maxResults, offset) and returns [pagination:{...}] footer"
    - "All three tools reject maxResults > 500 with INVALID_PARAMETER"
    - "All three tools reject maxResults <= 0 with INVALID_PARAMETER"
    - "list_assembly_types with namespaceFilter counts total as filtered set, not all types"
    - "list_assembly_types sorts types alphabetically by full name before slicing"
    - "search_members_by_name sorts matches alphabetically by declaring type then member name"
    - "list_embedded_resources sorts by resource name alphabetically"
  artifacts:
    - path: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      provides: "Pagination param validation for list_assembly_types"
      contains: "maxResults = 100"
    - path: "Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs"
      provides: "Pagination param validation for list_embedded_resources"
      contains: "maxResults = 100"
    - path: "Transport/Mcp/Tools/SearchMembersByNameTool.cs"
      provides: "Pagination param validation for search_members_by_name"
      contains: "maxResults = 100"
    - path: "Application/UseCases/ListAssemblyTypesUseCase.cs"
      provides: "Paginated type listing with PaginationEnvelope footer"
      contains: "PaginationEnvelope.AppendFooter"
    - path: "Application/UseCases/ListEmbeddedResourcesUseCase.cs"
      provides: "Paginated resource listing with PaginationEnvelope footer"
      contains: "PaginationEnvelope.AppendFooter"
    - path: "Application/UseCases/SearchMembersByNameUseCase.cs"
      provides: "Paginated member search with PaginationEnvelope footer"
      contains: "PaginationEnvelope.AppendFooter"
  key_links:
    - from: "Transport/Mcp/Tools/ListAssemblyTypesTool.cs"
      to: "Application/UseCases/ListAssemblyTypesUseCase.cs"
      via: "_useCase.ExecuteAsync with maxResults, offset"
      pattern: "ExecuteAsync.*maxResults.*offset"
    - from: "Application/UseCases/ListAssemblyTypesUseCase.cs"
      to: "Application/Pagination/PaginationEnvelope.cs"
      via: "PaginationEnvelope.AppendFooter"
      pattern: "PaginationEnvelope\\.AppendFooter"
---

<objective>
Add pagination to `list_assembly_types`, `list_embedded_resources`, and `search_members_by_name` following the established Phase 9/10 pagination contract pattern.

Purpose: These three list/search tools currently return unbounded results. Applying the pagination contract makes them safe for large assemblies and consistent with all other paginated tools.
Output: Three tools with (maxResults=100, offset=0) params, 500-cap validation, PaginationEnvelope footer, stable sort ordering, and pagination tests.
</objective>

<execution_context>
@C:\Users\cervo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\cervo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-list-get-search-pagination-member-enrichment/11-CONTEXT.md

<interfaces>
<!-- Canonical pagination helper — all paginated tools use this -->
From Application/Pagination/PaginationEnvelope.cs:
```csharp
public static class PaginationEnvelope
{
    public static void AppendFooter(StringBuilder sb, int total, int returned, int offset);
}
```

<!-- Reference: Transport-layer pagination validation pattern (verbatim from FindUsagesTool) -->
From Transport/Mcp/Tools/FindUsagesTool.cs:
```csharp
if (maxResults > 500)
    throw new McpToolException("INVALID_PARAMETER", "maxResults cannot exceed 500. Use offset to paginate.");
if (maxResults <= 0)
    throw new McpToolException("INVALID_PARAMETER", "maxResults must be >= 1.");
```

<!-- Reference: Use-case pagination pattern (from ListNamespaceTypesUseCase) -->
From Application/UseCases/ListNamespaceTypesUseCase.cs:
```csharp
public async Task<string> ExecuteAsync(string assemblyPath, string namespaceName,
    int maxResults = 100, int offset = 0, CancellationToken cancellationToken = default)
// ... inside limiter:
var page = allSorted.Skip(offset).Take(maxResults).ToList();
PaginationEnvelope.AppendFooter(sb, totalTopLevelTypes, returned, offset);
```

<!-- Current ListAssemblyTypesUseCase signature (no pagination yet) -->
From Application/UseCases/ListAssemblyTypesUseCase.cs:
```csharp
public async Task<string> ExecuteAsync(string assemblyPath, string? namespaceFilter,
    CancellationToken cancellationToken = default)
```

<!-- Current ListEmbeddedResourcesUseCase signature -->
From Application/UseCases/ListEmbeddedResourcesUseCase.cs:
```csharp
public async Task<string> ExecuteAsync(string assemblyPath,
    CancellationToken cancellationToken = default)
```

<!-- Current SearchMembersByNameUseCase signature -->
From Application/UseCases/SearchMembersByNameUseCase.cs:
```csharp
public async Task<string> ExecuteAsync(string assemblyPath, string searchTerm,
    string? memberKind, CancellationToken cancellationToken = default)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination to list_assembly_types and list_embedded_resources (PAGE-03)</name>
  <files>
    Transport/Mcp/Tools/ListAssemblyTypesTool.cs,
    Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs,
    Application/UseCases/ListAssemblyTypesUseCase.cs,
    Application/UseCases/ListEmbeddedResourcesUseCase.cs,
    Tests/Tools/ListAssemblyTypesToolTests.cs,
    Tests/Tools/ListEmbeddedResourcesToolTests.cs
  </files>
  <read_first>
    Transport/Mcp/Tools/ListAssemblyTypesTool.cs,
    Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs,
    Application/UseCases/ListAssemblyTypesUseCase.cs,
    Application/UseCases/ListEmbeddedResourcesUseCase.cs,
    Transport/Mcp/Tools/FindUsagesTool.cs,
    Application/UseCases/ListNamespaceTypesUseCase.cs,
    Application/Pagination/PaginationEnvelope.cs,
    Tests/Tools/ListAssemblyTypesToolTests.cs,
    Tests/Tools/ListEmbeddedResourcesToolTests.cs
  </read_first>
  <action>
**ListAssemblyTypesTool.cs** (per D-01, D-03):
- Add `[Description("Maximum number of results to return (default: 100)")] int maxResults = 100` and `[Description("Number of results to skip for pagination (default: 0)")] int offset = 0` parameters before `CancellationToken`.
- Add pagination validation block before `_useCase.ExecuteAsync` (verbatim from FindUsagesTool): `if (maxResults > 500) throw new McpToolException("INVALID_PARAMETER", "maxResults cannot exceed 500. Use offset to paginate."); if (maxResults <= 0) throw new McpToolException("INVALID_PARAMETER", "maxResults must be >= 1.");`
- Add `catch (McpToolException) { throw; }` as first catch clause.
- Pass `maxResults, offset` to `_useCase.ExecuteAsync`.

**ListAssemblyTypesUseCase.cs** (per D-01, D-02, D-04, D-10):
- Add `int maxResults = 100, int offset = 0` parameters to `ExecuteAsync` signature (before `CancellationToken`).
- Add `using ILSpy.Mcp.Application.Pagination;` and `using System.Text;` imports.
- Inside `_limiter.ExecuteAsync`, after getting `types` from `_decompiler.ListTypesAsync`:
  1. Sort types alphabetically: `var sorted = types.OrderBy(t => t.FullName, StringComparer.OrdinalIgnoreCase).ToList();`
  2. Compute total: `var total = sorted.Count;` (this is already filtered by namespaceFilter from the service layer, per D-04)
  3. Slice: `var page = sorted.Skip(offset).Take(maxResults).ToList();`
  4. Compute returned: `var returned = page.Count;`
  5. Build output with `StringBuilder`: header line `$"Assembly: {assembly.FileName}"`, then `$"Types found: {total}"`, blank line, then iterate `page` with `$"  {kind,-10} {type.FullName}"` per type.
  6. Append footer: `PaginationEnvelope.AppendFooter(result, total, returned, offset);`
  7. Return `result.ToString()`.

**ListEmbeddedResourcesTool.cs** (per D-01, D-03):
- Add `maxResults = 100` and `offset = 0` parameters with `[Description]` attributes (same text as FindUsagesTool).
- Add pagination validation block (same as above).
- Add `catch (McpToolException) { throw; }` as first catch clause.
- Pass `maxResults, offset` to `_useCase.ExecuteAsync`.

**ListEmbeddedResourcesUseCase.cs** (per D-01, D-02, D-13):
- Add `int maxResults = 100, int offset = 0` parameters to `ExecuteAsync`.
- Add `using ILSpy.Mcp.Application.Pagination;` import.
- Inside `_limiter.ExecuteAsync`, after getting `resources`:
  1. Sort alphabetically: `var sorted = resources.OrderBy(r => r.Name, StringComparer.OrdinalIgnoreCase).ToList();`
  2. `var total = sorted.Count;`
  3. `var page = sorted.Skip(offset).Take(maxResults).ToList();`
  4. `var returned = page.Count;`
  5. Pass `page` (not `resources`) to `FormatResources` and also pass `total`.
  6. Update `FormatResources` signature to `FormatResources(IReadOnlyList<ResourceInfo> resources, int total, int returned, int offset)`.
  7. In `FormatResources`: change header to `$"# Embedded Resources ({total})"`, keep existing resource formatting, append `PaginationEnvelope.AppendFooter(sb, total, returned, offset);` before the return.

**Tests** — Add 7 pagination tests to each test file following the Phase 9/10 pattern:
- `Pagination_DefaultReturnsFooter` — call with defaults, assert output contains `[pagination:` and `"truncated":false`
- `Pagination_MaxResultsCapsOutput` — call with `maxResults: 2`, assert returned <= 2 and footer present
- `Pagination_OffsetSkipsItems` — call with `offset: 1`, assert first item in default call is not first item in offset call
- `Pagination_TruncatedTrueWhenMoreExist` — call with `maxResults: 1` against assembly with >1 item, assert `"truncated":true`
- `Pagination_ExceedingCapRejectsWithInvalidParameter` — call with `maxResults: 501`, assert throws with "INVALID_PARAMETER"
- `Pagination_ZeroMaxResultsRejects` — call with `maxResults: 0`, assert throws "INVALID_PARAMETER"
- `Pagination_NegativeMaxResultsRejects` — call with `maxResults: -1`, assert throws "INVALID_PARAMETER"

Use the test assembly already used in existing tests (the TestTargets assembly). Follow the exact test class structure and `ToolTestFixture` pattern already in each test file.
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && dotnet test --filter "FullyQualifiedName~ListAssemblyTypesToolTests|FullyQualifiedName~ListEmbeddedResourcesToolTests" --no-restore -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - ListAssemblyTypesTool.cs contains `int maxResults = 100` and `int offset = 0` parameters
    - ListAssemblyTypesTool.cs contains `maxResults > 500` validation
    - ListAssemblyTypesTool.cs contains `catch (McpToolException) { throw; }` as first catch
    - ListEmbeddedResourcesTool.cs contains `int maxResults = 100` and `int offset = 0` parameters
    - ListEmbeddedResourcesTool.cs contains `maxResults > 500` validation
    - ListAssemblyTypesUseCase.cs contains `PaginationEnvelope.AppendFooter`
    - ListAssemblyTypesUseCase.cs contains `.OrderBy(t => t.FullName`
    - ListAssemblyTypesUseCase.cs contains `.Skip(offset).Take(maxResults)`
    - ListEmbeddedResourcesUseCase.cs contains `PaginationEnvelope.AppendFooter`
    - ListEmbeddedResourcesUseCase.cs contains `.OrderBy(r => r.Name`
    - Tests/Tools/ListAssemblyTypesToolTests.cs contains `Pagination_DefaultReturnsFooter`
    - Tests/Tools/ListAssemblyTypesToolTests.cs contains `Pagination_ExceedingCapRejectsWithInvalidParameter`
    - Tests/Tools/ListEmbeddedResourcesToolTests.cs contains `Pagination_DefaultReturnsFooter`
    - dotnet test for both test classes exits 0
  </acceptance_criteria>
  <done>Both list tools accept (maxResults, offset), validate at Transport boundary, sort before slicing, emit PaginationEnvelope footer, and have 7 pagination tests each passing</done>
</task>

<task type="auto">
  <name>Task 2: Add pagination to search_members_by_name (PAGE-05)</name>
  <files>
    Transport/Mcp/Tools/SearchMembersByNameTool.cs,
    Application/UseCases/SearchMembersByNameUseCase.cs,
    Tests/Tools/SearchMembersByNameToolTests.cs
  </files>
  <read_first>
    Transport/Mcp/Tools/SearchMembersByNameTool.cs,
    Application/UseCases/SearchMembersByNameUseCase.cs,
    Transport/Mcp/Tools/FindUsagesTool.cs,
    Application/Pagination/PaginationEnvelope.cs,
    Tests/Tools/SearchMembersByNameToolTests.cs
  </read_first>
  <action>
**SearchMembersByNameTool.cs** (per D-01, D-03):
- Add `[Description("Maximum number of results to return (default: 100)")] int maxResults = 100` and `[Description("Number of results to skip for pagination (default: 0)")] int offset = 0` parameters before `CancellationToken`.
- Add pagination validation block: `if (maxResults > 500) throw new McpToolException("INVALID_PARAMETER", "maxResults cannot exceed 500. Use offset to paginate."); if (maxResults <= 0) throw new McpToolException("INVALID_PARAMETER", "maxResults must be >= 1.");`
- Add `catch (McpToolException) { throw; }` as first catch clause.
- Pass `maxResults, offset` to `_useCase.ExecuteAsync`.

**SearchMembersByNameUseCase.cs** (per D-01, D-02, D-12):
- Add `int maxResults = 100, int offset = 0` parameters to `ExecuteAsync` (before `CancellationToken`).
- Add `using ILSpy.Mcp.Application.Pagination;` import.
- Inside `_limiter.ExecuteAsync`, after getting `results` from `_decompiler.SearchMembersAsync`:
  1. Sort: `var sorted = results.OrderBy(m => m.TypeFullName, StringComparer.OrdinalIgnoreCase).ThenBy(m => m.Name, StringComparer.OrdinalIgnoreCase).ToList();`  (per D-12: alphabetically by declaring type, then member name)
  2. `var total = sorted.Count;`
  3. `var page = sorted.Skip(offset).Take(maxResults).ToList();`
  4. `var returned = page.Count;`
  5. Build output: keep the existing `$"Search results for '{searchTerm}' in {assembly.FileName}"` header, then `$"Found {total} matching members:"`, blank line, then group `page` (not `results`) by `TypeFullName` and format each `[{member.Kind}] {member.Signature}` as before.
  6. Append `PaginationEnvelope.AppendFooter(result, total, returned, offset);`
  7. Return `result.ToString()`.

**Tests** — Add 7 pagination tests to SearchMembersByNameToolTests.cs:
- `Pagination_DefaultReturnsFooter` — search with a term that returns results, assert `[pagination:` in output
- `Pagination_MaxResultsCapsOutput` — search with `maxResults: 1`, verify returned <= 1
- `Pagination_OffsetSkipsItems` — search with `offset: 1`, verify first result differs from default call
- `Pagination_TruncatedTrueWhenMoreExist` — search with `maxResults: 1` against term with >1 match, assert `"truncated":true`
- `Pagination_ExceedingCapRejectsWithInvalidParameter` — `maxResults: 501`, assert throws "INVALID_PARAMETER"
- `Pagination_ZeroMaxResultsRejects` — `maxResults: 0`, assert throws "INVALID_PARAMETER"
- `Pagination_NegativeMaxResultsRejects` — `maxResults: -1`, assert throws "INVALID_PARAMETER"

Use a common search term like "Get" or "ToString" against the TestTargets assembly to guarantee multiple matches.
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && dotnet test --filter "FullyQualifiedName~SearchMembersByNameToolTests" --no-restore -v quiet</automated>
  </verify>
  <acceptance_criteria>
    - SearchMembersByNameTool.cs contains `int maxResults = 100` and `int offset = 0` parameters
    - SearchMembersByNameTool.cs contains `maxResults > 500` validation
    - SearchMembersByNameTool.cs contains `catch (McpToolException) { throw; }` as first catch
    - SearchMembersByNameUseCase.cs contains `PaginationEnvelope.AppendFooter`
    - SearchMembersByNameUseCase.cs contains `.OrderBy(m => m.TypeFullName`
    - SearchMembersByNameUseCase.cs contains `.Skip(offset).Take(maxResults)`
    - Tests/Tools/SearchMembersByNameToolTests.cs contains `Pagination_DefaultReturnsFooter`
    - Tests/Tools/SearchMembersByNameToolTests.cs contains `Pagination_ExceedingCapRejectsWithInvalidParameter`
    - dotnet test for SearchMembersByNameToolTests exits 0
  </acceptance_criteria>
  <done>search_members_by_name accepts (maxResults, offset), validates at Transport boundary, sorts by declaring type then member name, emits PaginationEnvelope footer, and has 7 pagination tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> Transport layer | Agent-supplied pagination params (maxResults, offset) are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | ListAssemblyTypesTool, ListEmbeddedResourcesTool, SearchMembersByNameTool | mitigate | Validate maxResults > 0 and <= 500 at Transport boundary before passing to use case (per D-03). Reject with INVALID_PARAMETER error code. |
| T-11-02 | Denial of Service | All three tools | mitigate | maxResults cap at 500 prevents unbounded memory allocation. Existing ITimeoutService and IConcurrencyLimiter provide timeout and concurrency protection. |
| T-11-03 | Information Disclosure | ListAssemblyTypesTool (namespaceFilter) | accept | namespaceFilter is case-insensitive substring match on server-local assemblies. No path traversal risk — assemblyPath is validated by AssemblyPath.Create(). Low risk: tool operates on assemblies the agent already has a path to. |
</threat_model>

<verification>
1. `dotnet test --filter "FullyQualifiedName~ListAssemblyTypesToolTests|FullyQualifiedName~ListEmbeddedResourcesToolTests|FullyQualifiedName~SearchMembersByNameToolTests"` — all tests pass
2. `grep -c "PaginationEnvelope.AppendFooter" Application/UseCases/ListAssemblyTypesUseCase.cs Application/UseCases/ListEmbeddedResourcesUseCase.cs Application/UseCases/SearchMembersByNameUseCase.cs` — each file shows count >= 1
3. `grep -c "maxResults > 500" Transport/Mcp/Tools/ListAssemblyTypesTool.cs Transport/Mcp/Tools/ListEmbeddedResourcesTool.cs Transport/Mcp/Tools/SearchMembersByNameTool.cs` — each file shows count >= 1
</verification>

<success_criteria>
- All three tools accept (maxResults=100, offset=0) with 500-cap validation
- All three use cases sort before slicing and emit PaginationEnvelope footer
- 21 new pagination tests (7 per tool) pass alongside all existing tests
- No regressions in existing tool tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-list-get-search-pagination-member-enrichment/11-01-SUMMARY.md`
</output>
