---
phase: 09-pagination-contract-structural-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PAGINATION.md
  - .claude/skills/mcp-tool-design/SKILL.md
autonomous: true
requirements: [PAGE-01]
must_haves:
  truths:
    - "A human or AI reader can find the single canonical pagination contract spec in one place."
    - "The mcp-tool-design skill Principle 4 points readers at the canonical doc instead of duplicating the rules."
  artifacts:
    - path: "docs/PAGINATION.md"
      provides: "Parameters, defaults, hard ceiling, rejection behavior, response envelope with footer schema, three worked examples (zero-match, mid-page, final-page), and a scope note listing which tools implement the contract."
      min_lines: 70
      contains: "[pagination:"
    - path: ".claude/skills/mcp-tool-design/SKILL.md"
      provides: "Principle 4 updated to cross-reference docs/PAGINATION.md as the canonical source of truth."
      contains: "docs/PAGINATION.md"
  key_links:
    - from: ".claude/skills/mcp-tool-design/SKILL.md"
      to: "docs/PAGINATION.md"
      via: "Principle 4 prose link"
      pattern: "docs/PAGINATION\\.md"
---

<objective>
Deliver PAGE-01: the single canonical pagination contract spec, and make the
mcp-tool-design skill cross-reference it so the rules never drift.

Purpose: Phases 10, 11, 12 will each apply the pagination contract to different
tools. They all need to cite ONE location instead of re-inventing or re-deriving
the shape. This plan is the source of truth every downstream phase links to.

Output:
- NEW `docs/PAGINATION.md` (clean slate — `docs/` currently contains only banner.png).
- UPDATED `.claude/skills/mcp-tool-design/SKILL.md` Principle 4 with a cross-reference line.

This plan is PURE DOCS — no C# code, no DI wiring, no tests. It runs in parallel
with 09-02 (CLEAN-01 dispatcher delete) in Wave 1 because the two plans touch
entirely disjoint files.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-CONTEXT.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-RESEARCH.md
@.claude/skills/mcp-tool-design/SKILL.md

<contract_verbatim>
<!-- These blocks are LOCKED by CONTEXT.md. The executor must transcribe them
     verbatim into docs/PAGINATION.md — do not paraphrase the footer shape,
     field order, or worked examples. -->

Pagination contract — response envelope format:

```
[tool-specific natural-language header]

[results body]

[pagination:{"total":N,"returned":N,"offset":N,"truncated":bool,"nextOffset":N|null}]
```

Footer rules:
- Single line, wrapped in `[pagination:...]`, single-line minified JSON.
- Field order is FIXED: `total`, `returned`, `offset`, `truncated`, `nextOffset`.
- Always present on every response from a paginable tool, including zero-match
  and final-page responses.
- `nextOffset` is a valid next offset when `truncated=true`, explicit `null`
  when `truncated=false`. Never omitted.

Parameters:
- `maxResults` (int, default `100`) — items per page.
- `offset` (int, default `0`) — items to skip.
- Hard ceiling: `maxResults <= 500`. Rejected with
  `McpToolException("INVALID_PARAMETER", "maxResults cannot exceed 500. Use offset to paginate.")`
- `maxResults <= 0` rejected with
  `McpToolException("INVALID_PARAMETER", "maxResults must be >= 1.")`
- `offset >= total` is NOT an error: empty body, `total` populated,
  `truncated=false`, `nextOffset=null`.

Worked examples (verbatim into doc):

```
// Zero matches
(no matching types found)

[pagination:{"total":0,"returned":0,"offset":0,"truncated":false,"nextOffset":null}]

// Mid-page
...body rows 1-100...

[pagination:{"total":523,"returned":100,"offset":0,"truncated":true,"nextOffset":100}]

// Final page
...body rows 501-523...

[pagination:{"total":523,"returned":23,"offset":500,"truncated":false,"nextOffset":null}]
```

Scope note for the doc:
- Phase 9 (v1.2.0): `list_namespace_types` is the first and only implementer.
- Phase 10: all `find_*` tools (PAGE-02).
- Phase 11: `list_assembly_types`, `list_embedded_resources`, `get_type_members`,
  `search_members_by_name` (PAGE-03..05). Note: PAGE-06 (`list_namespace_types`)
  landed in Phase 9 — see plan 09-04 for the roadmap ripple update.
- Phase 12: `search_strings` and `search_constants` get the trailing footer added
  (OUTPUT-06, OUTPUT-07 — they already expose `maxResults`/`offset`).
- Source-returning / bounded-output tools (PAGE-07, PAGE-08) use a related but
  simpler `(truncated, total_lines)` pattern tracked separately.
</contract_verbatim>

<skill_principle_4_current>
<!-- Current SKILL.md Principle 4 (lines 54-67). The executor must add a
     cross-reference line that points at docs/PAGINATION.md. Do NOT rewrite the
     principle itself — the rules stay where they are; only the pointer is new. -->
From .claude/skills/mcp-tool-design/SKILL.md lines 54-67:

### 4. Pagination is mandatory for unbounded output (HARD RULE)

**Rule:** Any tool whose output count depends on the input assembly's contents...
**must** expose pagination parameters. The output **must** report `truncated`
and `total` so the agent knows whether more pages exist.

**Why:** Without pagination an agent gets either (a) silent truncation that
corrupts its mental model of the assembly, or (b) a multi-megabyte blob that
destroys its context window.

```csharp
// REQUIRED parameters whenever output is unbounded:
int maxResults = 100,        // sensible default
int offset = 0,              // for cursor-style continuation
// ...and the response must include `truncated: bool` and `total: int`.
```

A tool whose output is structurally bounded... is exempt...
</skill_principle_4_current>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/PAGINATION.md with the canonical contract spec</name>
  <files>docs/PAGINATION.md</files>
  <action>
Create the NEW file `docs/PAGINATION.md`. The `docs/` directory already exists
(contains only `banner.png`), so this is a clean-slate file creation.

Structure the document with these sections IN ORDER:

1. **Title + one-paragraph intro** — "This document defines the single, uniform
   pagination contract that every list-returning MCP tool in this project
   obeys. It is the canonical reference; the skill at
   `.claude/skills/mcp-tool-design/SKILL.md` Principle 4 and `README.md` both
   link here rather than duplicate the rules."

2. **Parameters** — the two parameters with their types, defaults, and purpose.
   Use the exact parameter names (`maxResults`, `offset`), the exact defaults
   (100, 0), and the exact `[Description]` strings already in use by
   `SearchStringsTool` / `SearchConstantsTool`:
   - "Maximum number of results to return (default: 100)"
   - "Number of results to skip for pagination (default: 0)"

3. **Hard Ceiling** — the 500 ceiling and the `maxResults <= 0` rejection.
   Include the verbatim `McpToolException("INVALID_PARAMETER", ...)` strings
   from the context block above. Explain that rejection (not silent clamping)
   is intentional — agents discover the limit on first violation.

4. **Response Envelope** — the three-section shape (header, body, footer) with
   the exact format block from the contract_verbatim context above.

5. **Footer Shape** — a field-by-field table:
   | Field | Type | Meaning |
   |-------|------|---------|
   | `total` | int | Total items across all pages (unsliced result size) |
   | `returned` | int | Items actually in this response's body |
   | `offset` | int | Offset this response was computed at |
   | `truncated` | bool | true if more items exist beyond this page |
   | `nextOffset` | int or null | valid offset when truncated=true, explicit null when truncated=false |

6. **Edge Cases** — bulleted: zero matches (`total=0`), final page, offset
   beyond total (NOT an error — tool returns empty body with populated `total`).

7. **Worked Examples** — transcribe all THREE examples (zero-match, mid-page,
   final-page) from the contract_verbatim block above VERBATIM. Do not
   paraphrase, do not reorder, do not add or remove whitespace inside the JSON.

8. **Scope** — the phase-by-phase scope note from the contract_verbatim block
   above. Call out that `list_namespace_types` is the first and only Phase 9
   implementer and that subsequent phases will cite this doc rather than
   re-deriving the shape.

Use plain markdown. No code evaluator. No broken links. The file must satisfy
all these greps after creation:

- `grep -q "\[pagination:" docs/PAGINATION.md` — must succeed
- `grep -q "maxResults" docs/PAGINATION.md` — must succeed
- `grep -q "nextOffset" docs/PAGINATION.md` — must succeed
- `grep -q "500" docs/PAGINATION.md` — must succeed (ceiling)
- `grep -q "list_namespace_types" docs/PAGINATION.md` — must succeed (scope)

Recommended prose length: ~80-120 lines total. The spec is short; do not
pad with philosophy. The three worked examples together are roughly half the
file.
  </action>
  <verify>
    <automated>test -f docs/PAGINATION.md && grep -q "\[pagination:" docs/PAGINATION.md && grep -q "maxResults" docs/PAGINATION.md && grep -q "nextOffset" docs/PAGINATION.md && grep -q "500" docs/PAGINATION.md && grep -q "list_namespace_types" docs/PAGINATION.md</automated>
  </verify>
  <done>
docs/PAGINATION.md exists. Contains all five required tokens (`[pagination:`,
`maxResults`, `nextOffset`, `500`, `list_namespace_types`). Worked examples
match CONTEXT.md verbatim. File is readable as standard markdown with no
broken formatting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference PAGINATION.md from mcp-tool-design skill Principle 4</name>
  <files>.claude/skills/mcp-tool-design/SKILL.md</files>
  <action>
Edit `.claude/skills/mcp-tool-design/SKILL.md` to add a cross-reference line to
`docs/PAGINATION.md` inside Principle 4 (lines 54-67 in the current file).

DO NOT rewrite Principle 4's rule or rationale. The existing prose at lines
54-67 stays exactly as-is. Only ADD ONE new line (or short paragraph) inside
that principle pointing to the canonical contract doc.

Recommended placement: immediately after the existing code block at lines
60-65 (the `int maxResults = 100, int offset = 0` block), and before the
"A tool whose output is structurally bounded..." paragraph at line 67. Add
a new paragraph such as:

> **Canonical format:** The exact response envelope (header, body, trailing
> `[pagination:...]` footer), the hard ceiling (500), and the rejection
> behavior are specified in **[`docs/PAGINATION.md`](../../../docs/PAGINATION.md)**.
> Every paginable tool in this project obeys that contract — do not invent a
> new shape.

The relative path `../../../docs/PAGINATION.md` is correct: the skill lives at
`.claude/skills/mcp-tool-design/SKILL.md`, so going up three directories lands
at the repo root, then into `docs/`. The executor MUST verify the relative
path by eye — if the user's global skills dir is symlinked or the layout
differs, fall back to the literal string `docs/PAGINATION.md` without the
relative prefix. The grep verification only checks for the substring
`docs/PAGINATION.md`, so a plain string reference also satisfies the contract.

Do not touch any other principle (Principles 1-3, 5-8 stay verbatim). Do not
rewrite the checklist. Do not change the "Reference" section at the bottom —
that section lists three reference locations (audit, README, tool source) and
adding PAGINATION.md there would be appropriate but is not required by this
plan's must_haves. Executor's discretion: may also add it to the Reference
section bullet list if it keeps the skill cohesive.

Preserve every other byte of the file. The skill is long — use targeted edits,
not a rewrite.
  </action>
  <verify>
    <automated>grep -q "docs/PAGINATION.md" .claude/skills/mcp-tool-design/SKILL.md && grep -A 20 "### 4. Pagination" .claude/skills/mcp-tool-design/SKILL.md | grep -q "docs/PAGINATION.md"</automated>
  </verify>
  <done>
SKILL.md Principle 4 now contains a reference to `docs/PAGINATION.md`. The
rest of the skill file is unchanged (every other principle and the checklist
are byte-identical to the previous version). The grep confirms the reference
lives inside Principle 4, not accidentally appended to an unrelated section.
  </done>
</task>

</tasks>

<verification>
Plan-level success gate:

1. `test -f docs/PAGINATION.md` returns 0.
2. All 5 required tokens appear in PAGINATION.md (see Task 1 verify).
3. `grep -q "docs/PAGINATION.md" .claude/skills/mcp-tool-design/SKILL.md` returns 0.
4. The reference is inside Principle 4 (not a different principle). Quick
   check: `grep -A 20 "### 4. Pagination" .claude/skills/mcp-tool-design/SKILL.md | grep -q "docs/PAGINATION.md"` returns 0.
5. `dotnet build ILSpy.Mcp.sln` still passes (no C# changes, should be a no-op
   build). Run as a guardrail to catch accidental edits.

Manual sanity check (executor reads aloud to self):
- Are the three worked examples in PAGINATION.md byte-for-byte identical to
  the ones in CONTEXT.md (zero-match, mid-page, final-page)? If there is any
  divergence (even whitespace), re-copy from CONTEXT.md.
- Does Principle 4's addition read as a natural extension of the existing
  prose, not a drive-by mention?
</verification>

<success_criteria>
- docs/PAGINATION.md exists and is the canonical source of truth for the
  pagination contract (PAGE-01 phase success criterion #1).
- The mcp-tool-design skill points at the canonical doc, preventing drift
  between the skill's Principle 4 and the contract as applied to tools in
  Phases 9-12.
- Zero code changes. Zero test changes. This plan is pure docs.
- Executes in parallel with plan 09-02 (CLEAN-01 delete) because the two
  plans touch completely disjoint files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-pagination-contract-structural-cleanup/09-01-SUMMARY.md`
recording:
- `requirements-completed: [PAGE-01]`
- Files created/modified (exactly 2)
- Any deviations from the verbatim worked examples in CONTEXT.md (expected: none)
- Note whether the skill's Reference section also received the PAGINATION.md
  entry (executor's discretion, not required).
</output>
