---
phase: 09-pagination-contract-structural-cleanup
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CLEAN-03]
must_haves:
  truths:
    - "README.md reflects exactly 27 tools (no `28 tools` references) and the v1.2 surface (no `analyze_references` or `decompile_namespace`)."
    - "README.md has a `#### \\`list_namespace_types\\`` section with `maxResults` and `offset` parameters documented in the parameter table."
    - "README.md links to `docs/PAGINATION.md` from a brief Pagination intro paragraph near the top of the tool catalogue."
    - "REQUIREMENTS.md PAGE-06 traceability row at line 114 reflects 'Phase 9' instead of 'Phase 11'."
    - "REQUIREMENTS.md PAGE-06 bullet at line 20 is rewritten to indicate it landed in Phase 9 alongside CLEAN-02."
    - "ROADMAP.md Phase 11 Requirements line (line 80) no longer lists PAGE-06."
    - "ROADMAP.md Phase 11 success criterion #4 (line 85) no longer mentions `maxTypes=200` (the cap that was replaced is now described in past tense or moved to Phase 9)."
  artifacts:
    - path: "README.md"
      provides: "Updated tool catalogue: 27 tools, list_namespace_types section, no analyze_references / decompile_namespace, Pagination intro linking to docs/PAGINATION.md."
      contains: "list_namespace_types"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PAGE-06 row in traceability table credits Phase 9; PAGE-06 bullet under Pagination section reflects landing in Phase 9 with CLEAN-02."
      contains: "PAGE-06"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 Requirements list and success criteria updated to drop PAGE-06 (now landed in Phase 9)."
      contains: "Phase 11"
  key_links:
    - from: "README.md"
      to: "docs/PAGINATION.md"
      via: "Markdown link in Pagination intro paragraph at top of tool catalogue"
      pattern: "docs/PAGINATION\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 9 (CLEAN-02 + PAGE-06)"
      via: "PAGE-06 traceability row"
      pattern: "PAGE-06.*Phase 9"
---

<objective>
Deliver CLEAN-03 (README sync) AND the roadmap ripple from Plan 09-03's
scope expansion: PAGE-06 (`list_namespace_types` pagination) landed in
Phase 9 instead of Phase 11.

Three concrete deliverables:

1. **CLEAN-03 — README.md update.** Tool count 28 → 27, delete the
   `analyze_references` section, rename + update the `decompile_namespace`
   section to `list_namespace_types` with new `maxResults` / `offset`
   parameters, add a Pagination intro paragraph linking to
   `docs/PAGINATION.md`. Do NOT rewrite any other tool descriptions —
   Phase 13 owns the description sweep.

2. **Roadmap ripple in REQUIREMENTS.md.** Move PAGE-06 from Phase 11 to
   Phase 9 in the traceability table (line 114) and rewrite the bullet
   at line 20 to reflect the landing.

3. **Roadmap ripple in ROADMAP.md.** Update Phase 11's Requirements line
   (line 80) and success criterion #4 (line 85) to drop PAGE-06 / the
   `maxTypes=200` reference.

Purpose: With Plans 09-01 (docs), 09-02 (delete dispatcher), and 09-03
(rename + pagination implementation) all complete, the running tool
surface is final. This plan brings the user-facing README and the
planning artifacts in sync with reality.

This is the LAST plan in Phase 9. After this plan lands, all four phase
success criteria are met. Wave 3 — strict dependency on Plans 09-01,
09-02, AND 09-03 because:
- README needs the final tool surface (depends on 09-02 + 09-03).
- README's Pagination intro paragraph links to `docs/PAGINATION.md`,
  which is created by 09-01.
- The roadmap ripple records that PAGE-06 landed in Phase 9 — that
  landing happens in 09-03.

## Surfaced deviation: roadmap ripple

This plan modifies `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to
reflect a Phase-9 scope expansion that was decided in `09-CONTEXT.md`:

- **Original roadmap (lines 80, 85, 114):** PAGE-06 (`list_namespace_types`
  pagination) was scheduled for Phase 11. ROADMAP.md success criterion #4
  for Phase 11 still mentions "the previous hard `maxTypes=200` cap".
- **Phase 9 reality:** Plan 09-03 implements PAGE-06 as part of CLEAN-02's
  rename, because doing the pagination at the same time as the rename is
  cheaper than rewriting the same tool twice across two phases.
- **Edits needed (with exact line numbers from 09-RESEARCH.md):**
  - `.planning/REQUIREMENTS.md` line 20 (the PAGE-06 bullet under
    Pagination section): rewrite to indicate the requirement landed in
    Phase 9 alongside CLEAN-02.
  - `.planning/REQUIREMENTS.md` line 114 (the PAGE-06 row in the
    traceability table): change "Phase 11" to "Phase 9".
  - `.planning/ROADMAP.md` line 80 (Phase 11 Requirements list): remove
    `PAGE-06` from the bracketed list.
  - `.planning/ROADMAP.md` line 85 (Phase 11 success criterion #4): drop
    the line entirely OR rewrite to refer to a different remaining
    Phase 11 concern. RECOMMENDATION: drop the `list_namespace_types`
    success criterion entirely from Phase 11 since the work is done; the
    remaining 4 success criteria for Phase 11 still cover
    `list_assembly_types`, `list_embedded_resources`, `get_type_members`,
    `search_members_by_name`, and `get_type_members` enrichment, which
    is enough.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-CONTEXT.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-RESEARCH.md
@docs/PAGINATION.md
@README.md

<readme_anchors>
<!-- Exact README.md line numbers and current text. Verified from current
     working tree. Executor MUST re-grep before editing in case earlier plans
     in the wave have shifted lines (unlikely — none of 09-01/09-02/09-03
     touch README.md). -->

Line 58 (Features intro):
```
**28 tools** across 8 categories:
```

Line 376 (decompile_namespace section heading):
```
#### `decompile_namespace`
```

Line 378 (the description sentence — DO NOT REWRITE the description; only
fix the parameter name reference):
```
List all types in a namespace with full signatures, member counts, and public method signatures. Returns a summary -- use `decompile_type` to get full source for individual types.
```

Lines 380-384 (parameter table):
```
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `assemblyPath` | string | Yes | Path to the .NET assembly file |
| `namespaceName` | string | Yes | Full namespace name (e.g., `System.Collections.Generic`) |
| `maxTypes` | int | No | Maximum number of types to return (default: 200) |
```

Line 884 (analyze_references section heading) and following ~30 lines (the
section body, parameter table, example, ending around line 915):
```
#### `analyze_references`
... description ...
| Parameter | Type | Required | Description |
| ... rows ... |
<details>...example...</details>
```

The `---` separator at line 882 (before the analyze_references heading) and
line 917 (after the example) bracket the deletable region.
</readme_anchors>

<requirements_anchors>
<!-- REQUIREMENTS.md line 20 (current — PAGE-06 bullet): -->
- [ ] **PAGE-06**: `list_namespace_types` (renamed from `decompile_namespace`) implements PAGE-01 contract (replaces the `maxTypes=200` hard cap)

<!-- REQUIREMENTS.md line 114 (current — PAGE-06 row in traceability table): -->
| PAGE-06 | Phase 11 | Pending |

<!-- The two edits this plan makes: -->
<!-- Line 20 → -->
- [x] **PAGE-06**: `list_namespace_types` implements PAGE-01 contract (replaces the previous `maxTypes=200` hard cap) — landed in Phase 9 alongside CLEAN-02

<!-- Line 114 → -->
| PAGE-06 | Phase 9 | Complete |
</requirements_anchors>

<roadmap_anchors>
<!-- ROADMAP.md line 80 (current — Phase 11 Requirements line): -->
**Requirements**: PAGE-03, PAGE-04, PAGE-05, PAGE-06, OUTPUT-05

<!-- After edit: -->
**Requirements**: PAGE-03, PAGE-04, PAGE-05, OUTPUT-05

<!-- ROADMAP.md line 85 (current — Phase 11 success criterion #4): -->
  4. `list_namespace_types` (renamed) uses the pagination contract in place of the previous hard `maxTypes=200` cap - verifiable by calling it against a namespace with >200 types and confirming no silent truncation

<!-- After edit: REMOVE this line entirely. The other success criteria
     #1, #2, #3, #5 are renumbered: #5 becomes #4. The Phase 11 plans count
     stays at 4 success criteria post-removal. -->
</roadmap_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md — tool count, delete analyze_references section, rename + update decompile_namespace section, add Pagination intro</name>
  <files>README.md</files>
  <action>
Four atomic edits to README.md. All edits in one commit.

**Edit 1 — Tool count (line 58):**

Current:
```
**28 tools** across 8 categories:
```

New:
```
**27 tools** across 8 categories:
```

Re-locate via grep if line number drifted: `grep -n "28 tools" README.md`.

**Edit 2 — Add Pagination intro paragraph near top of tool catalogue:**

Locate the start of the tool catalogue. Search for the first `### ` heading
after the "Supported MCP Clients" / "What is this?" intro section. The
catalogue typically begins with a heading like `### Decompilation` around
line 200-300 of the README.

Add a NEW paragraph immediately BEFORE the first `### ` catalogue heading.
Recommended text:

```markdown
> **Pagination:** Tools that return lists (e.g., `list_namespace_types`,
> `find_*`, `search_*`) accept `maxResults` and `offset` parameters with
> sensible defaults and always include a trailing `[pagination:{...}]`
> footer reporting `total`, `returned`, `truncated`, and `nextOffset`.
> See [`docs/PAGINATION.md`](docs/PAGINATION.md) for the complete contract,
> including the hard ceiling and worked examples.
```

This is a brief intro — DO NOT duplicate the contract content here. The
canonical spec is in `docs/PAGINATION.md` (created by Plan 09-01).

**Edit 3 — Rename + update the `decompile_namespace` section (lines ~376-415):**

Current section heading at line 376:
```
#### `decompile_namespace`
```

New:
```
#### `list_namespace_types`
```

The description sentence at line 378 mentions `decompile_type` (a different
tool, NOT being renamed). Update the sentence ONLY to fix the tool name
reference (no description rewrite — Phase 13 owns descriptions):

Current line 378:
```
List all types in a namespace with full signatures, member counts, and public method signatures. Returns a summary -- use `decompile_type` to get full source for individual types.
```

Keep this sentence verbatim — `decompile_type` is correct, this section
just describes a different (sibling) tool. The reference to `decompile_type`
stays. Only the section heading changes.

The parameter table (lines 380-384) needs the `maxTypes` row replaced with
TWO rows for `maxResults` and `offset`:

Current:
```
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `assemblyPath` | string | Yes | Path to the .NET assembly file |
| `namespaceName` | string | Yes | Full namespace name (e.g., `System.Collections.Generic`) |
| `maxTypes` | int | No | Maximum number of types to return (default: 200) |
```

New:
```
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `assemblyPath` | string | Yes | Path to the .NET assembly file |
| `namespaceName` | string | Yes | Full namespace name (e.g., `System.Collections.Generic`) |
| `maxResults` | int | No | Maximum number of results to return (default: 100) |
| `offset` | int | No | Number of results to skip for pagination (default: 0) |
```

The example block under `<details>` (lines 386-415 approximately) shows
sample input/output JSON. The input does not pass `maxTypes`, so no edit
needed there. The output example shows formatted text — verify it doesn't
mention `maxTypes` or the old tool name. If the example output contains the
text "Namespace: ... (X types)" matching the OLD header format, leave it —
the example is illustrative and the new format is documented in
`docs/PAGINATION.md`. If the example explicitly references the old
`maxTypes=200` cap or shows a `decompile_namespace` invocation, update it.

**Edit 4 — Delete the `analyze_references` section (lines ~882-917):**

Locate the section start: `grep -n "#### \`analyze_references\`" README.md`.

The section spans from the `---` separator before the heading at line 882
through the closing `</details>` at line ~915 and the next `---` separator at
line ~917. Delete the WHOLE section including the leading `---` separator.

Be careful NOT to delete the `---` separator that follows — that one
separates the (now-deleted) analyze_references section from the next section
(`### Assembly Inspection` at line 919) and that next section's separator
must remain.

After deletion, the structure should be:
```
... preceding section ends ...

---

### Assembly Inspection
... content ...
```

Verify by re-reading the area: the section heading directly before "###
Assembly Inspection" should be a different tool's `####` heading, not
`#### \`analyze_references\``.

**Sanity grep after all four edits:**
```
grep -n "28 tools" README.md          # expect: zero hits
grep -n "27 tools" README.md          # expect: 1 hit (line ~58)
grep -n "decompile_namespace" README.md  # expect: zero hits
grep -n "analyze_references" README.md   # expect: zero hits
grep -n "list_namespace_types" README.md # expect: at least 1 hit (the section heading)
grep -n "docs/PAGINATION.md" README.md   # expect: at least 1 hit (the intro paragraph)
grep -n "maxResults" README.md           # expect: at least 1 hit (the parameter table; possibly more if other tools' tables already mention it)
```

All five grep expectations must be met. If any fails, re-locate and re-edit.
  </action>
  <verify>
    <automated>! grep -q "28 tools" README.md && grep -q "27 tools" README.md && ! grep -q "decompile_namespace" README.md && ! grep -q "analyze_references" README.md && grep -q "#### \`list_namespace_types\`" README.md && grep -q "docs/PAGINATION.md" README.md && grep -q "| \`maxResults\` | int |" README.md && grep -q "| \`offset\` | int |" README.md</automated>
  </verify>
  <done>
- README tool count is 27, not 28.
- No occurrences of `analyze_references` or `decompile_namespace` anywhere
  in README.md.
- `list_namespace_types` section heading exists with `maxResults` AND
  `offset` rows in its parameter table.
- A Pagination intro paragraph links to `docs/PAGINATION.md` near the top
  of the tool catalogue.
- No other tool descriptions touched (Phase 13 territory). Verified by
  reading the diff and confirming all changes are confined to the four
  intended edit regions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and ROADMAP.md for the PAGE-06 ripple</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Three planning-document edits documenting that PAGE-06 landed in Phase 9
instead of Phase 11.

**Edit 1 — REQUIREMENTS.md line 20 (the PAGE-06 bullet):**

Current:
```
- [ ] **PAGE-06**: `list_namespace_types` (renamed from `decompile_namespace`) implements PAGE-01 contract (replaces the `maxTypes=200` hard cap)
```

New:
```
- [x] **PAGE-06**: `list_namespace_types` implements PAGE-01 contract (replaces the previous `maxTypes=200` hard cap) — landed in Phase 9 alongside CLEAN-02
```

Re-locate via grep if line drifted: `grep -n "PAGE-06" .planning/REQUIREMENTS.md`.

The change marks the bullet as complete (`[x]`), removes the redundant
"renamed from `decompile_namespace`" parenthetical (the rename is now in the
past), and adds a "landed in Phase 9" suffix. The `maxTypes=200` reference
becomes "previous" tense.

**Edit 2 — REQUIREMENTS.md line 114 (the PAGE-06 traceability row):**

Current:
```
| PAGE-06 | Phase 11 | Pending |
```

New:
```
| PAGE-06 | Phase 9 | Complete |
```

Re-locate via grep if line drifted: `grep -n "| PAGE-06 |" .planning/REQUIREMENTS.md`.

This updates both the phase column AND the status column. The traceability
table will now show 1 fewer requirement under Phase 11 and 1 more under
Phase 9. (Note: the table doesn't aggregate counts, just lists rows, so no
header counts to update.)

**Edit 3 — ROADMAP.md line 80 (Phase 11 Requirements line):**

Current:
```
**Requirements**: PAGE-03, PAGE-04, PAGE-05, PAGE-06, OUTPUT-05
```

New:
```
**Requirements**: PAGE-03, PAGE-04, PAGE-05, OUTPUT-05
```

Re-locate via grep if line drifted: `grep -n "PAGE-03, PAGE-04, PAGE-05, PAGE-06" .planning/ROADMAP.md`.

The change removes `PAGE-06,` (and the trailing comma+space).

**Edit 4 — ROADMAP.md line 85 (Phase 11 success criterion #4):**

Current:
```
  4. `list_namespace_types` (renamed) uses the pagination contract in place of the previous hard `maxTypes=200` cap - verifiable by calling it against a namespace with >200 types and confirming no silent truncation
```

Re-locate via grep: `grep -n "list_namespace_types.*renamed.*pagination" .planning/ROADMAP.md`
or simply `grep -n "maxTypes=200" .planning/ROADMAP.md` (should find this
line, possibly the only hit in ROADMAP.md).

DELETE this entire criterion line. Renumber the remaining criteria so that
the criteria list stays contiguous (1, 2, 3, 4 → was 1, 2, 3, 5 with #4
removed; promote #5 to #4).

Looking at the current ROADMAP.md Phase 11 success criteria block (lines
~81-87):
```
**Success Criteria** (what must be TRUE):
  1. An agent calling `list_assembly_types` or `list_embedded_resources` ...
  2. An agent calling `get_type_members` ...
  3. An agent calling `search_members_by_name` ...
  4. `list_namespace_types` (renamed) uses the pagination contract ...   <-- DELETE
  5. A `get_type_members` response distinguishes inherited vs declared ...
```

After delete + renumber:
```
**Success Criteria** (what must be TRUE):
  1. An agent calling `list_assembly_types` or `list_embedded_resources` ...
  2. An agent calling `get_type_members` ...
  3. An agent calling `search_members_by_name` ...
  4. A `get_type_members` response distinguishes inherited vs declared ...
```

Phase 11 still has 4 success criteria (was 5; one moved to Phase 9's
success criteria). Phase 11's `**Plans**: TBD` line is unchanged.

**Final sanity grep across REQUIREMENTS.md and ROADMAP.md:**
```
grep -n "PAGE-06" .planning/REQUIREMENTS.md     # expect: 2 hits — line 20 (now [x] landed in Phase 9) and line 114 (now Phase 9 / Complete)
grep -n "PAGE-06" .planning/ROADMAP.md          # expect: 0 hits (was on line 80, removed)
grep -n "maxTypes=200" .planning/ROADMAP.md     # expect: 0 hits (was on line 85, line removed)
grep -n "maxTypes" .planning/ROADMAP.md         # expect: 0 hits across all of ROADMAP.md (sanity)
grep -n "PAGE-03, PAGE-04, PAGE-05, OUTPUT-05" .planning/ROADMAP.md  # expect: 1 hit (the rewritten Phase 11 Requirements line)
```

All five grep expectations must be met.
  </action>
  <verify>
    <automated>grep -q "PAGE-06.*Phase 9" .planning/REQUIREMENTS.md && grep -q "\[x\] \*\*PAGE-06\*\*" .planning/REQUIREMENTS.md && ! grep -q "PAGE-06" .planning/ROADMAP.md && ! grep -q "maxTypes" .planning/ROADMAP.md && grep -q "PAGE-03, PAGE-04, PAGE-05, OUTPUT-05" .planning/ROADMAP.md</automated>
  </verify>
  <done>
- REQUIREMENTS.md PAGE-06 bullet (line ~20) is checked off and credits
  Phase 9.
- REQUIREMENTS.md PAGE-06 traceability row (line ~114) reads "Phase 9 |
  Complete".
- ROADMAP.md Phase 11 Requirements line no longer lists PAGE-06.
- ROADMAP.md no longer mentions `maxTypes` anywhere.
- Phase 11 success criteria are renumbered 1-4 (was 1-5; the
  list_namespace_types criterion was deleted, the get_type_members
  enrichment criterion was promoted from #5 to #4).
- The roadmap ripple is fully recorded.
  </done>
</task>

</tasks>

<verification>
Plan-level success gate (also covers all four phase success criteria):

1. **Phase success criterion #1 (PAGE-01 doc):**
   - `test -f docs/PAGINATION.md` (delivered by 09-01).
2. **Phase success criterion #2 (no analyze_references):**
   - `! grep -q "analyze_references" README.md`
   - `! grep -q "AnalyzeReferencesTool" Program.cs`
   - `! grep -q "AnalyzeReferencesTool" Tests/Fixtures/ToolTestFixture.cs`
3. **Phase success criterion #3 (list_namespace_types present, decompile_namespace absent):**
   - `grep -q "#### \`list_namespace_types\`" README.md`
   - `! grep -q "decompile_namespace" README.md`
4. **Phase success criterion #4 (README tool count = 27, no stale references):**
   - `grep -q "27 tools" README.md && ! grep -q "28 tools" README.md`
   - `! grep -q "analyze_references\|decompile_namespace" README.md`
5. **Roadmap ripple:**
   - `grep -q "PAGE-06.*Phase 9" .planning/REQUIREMENTS.md`
   - `! grep -q "PAGE-06" .planning/ROADMAP.md`
   - `! grep -q "maxTypes" .planning/ROADMAP.md`
6. **Build still green (sanity, since this plan is docs-only):**
   - `dotnet build ILSpy.Mcp.sln --nologo -v minimal` exits 0.
7. **Test suite still passes (sanity):**
   - `dotnet test ILSpy.Mcp.sln --nologo -v minimal` reports
     `Passed: 175, Failed: 0` (175 = 168 post-09-02 + 7 new in 09-03).

Manual sanity check:
- Open README.md and read the tool catalogue end-to-end. Confirm 27 `####`
  tool headings across all categories. (The categories are listed at line
  56-67; eyeball the count by section.)
- Open `docs/PAGINATION.md` and verify the file exists and is readable.
- Read `.planning/REQUIREMENTS.md` PAGE-06 bullet and traceability row to
  confirm the wording reads naturally.
</verification>

<success_criteria>
- All four Phase 9 success criteria from ROADMAP.md are met:
  1. `docs/PAGINATION.md` exists with the contract spec. (delivered: 09-01)
  2. `tools/list` exposes 27 tools; `analyze_references` is absent.
     (delivered: 09-02 + this plan's README sync)
  3. `tools/list` exposes `list_namespace_types`; `decompile_namespace` is
     absent. (delivered: 09-03 + this plan's README sync)
  4. README.md reflects the 27-tool surface with no stale references.
     (delivered: this plan)
- The roadmap ripple from CONTEXT.md is recorded: PAGE-06 is now credited
  to Phase 9 in REQUIREMENTS.md and removed from Phase 11 in ROADMAP.md.
- This plan is the LAST in Phase 9. After it lands, Phase 9 is complete.
- Wave 3 — depends on 09-01 (for PAGINATION.md link target), 09-02 (for
  README/grep state to drop analyze_references), and 09-03 (for
  README/grep state to switch from decompile_namespace to
  list_namespace_types and to confirm PAGE-06 has actually landed before
  recording it as such in REQUIREMENTS.md).
- Build and tests stay green (this plan is pure docs/planning edits, no
  C# changes; the build/test gates are sanity checks).
</success_criteria>

<output>
After completion, create `.planning/phases/09-pagination-contract-structural-cleanup/09-04-SUMMARY.md`
recording:
- `requirements-completed: [CLEAN-03]`
- Files modified: 3 (README.md, REQUIREMENTS.md, ROADMAP.md)
- All four Phase 9 success criteria gates passed (with the exact greps used).
- **Roadmap ripple recorded:** PAGE-06 moved from Phase 11 to Phase 9; Phase
  11 success criteria reduced from 5 to 4; the `maxTypes=200` reference
  removed from ROADMAP.md.
- Confirmation that the test suite is still at 175/175 green and the build
  is still clean — pure-docs plans should not regress C# state.
- Note for the next planner (Phase 10): the pagination contract is locked,
  the canonical reference implementation is `list_namespace_types`, and
  Plan 09-03 deferred extracting a `PaginationEnvelope` helper. Phase 10
  applies the contract to all `find_*` tools and is the natural place to
  introduce the helper as the second consumer pattern emerges.
- After this SUMMARY lands, mark Phase 9 as complete in STATE.md (if the
  executor's standard workflow does not handle this automatically).
</output>
