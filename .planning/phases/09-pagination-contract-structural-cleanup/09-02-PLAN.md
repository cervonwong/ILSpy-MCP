---
phase: 09-pagination-contract-structural-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Transport/Mcp/Tools/AnalyzeReferencesTool.cs
  - Tests/Tools/AnalyzeReferencesToolTests.cs
  - Program.cs
  - Tests/Fixtures/ToolTestFixture.cs
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "`tools/list` exposes exactly 27 tools (was 28); `analyze_references` is not in the list."
    - "No source file, test file, or DI registration references `AnalyzeReferencesTool` or `analyze_references` anywhere in Transport/, Application/, Tests/, Domain/, Infrastructure/, or Program.cs."
    - "The four individual `find_*` tools (`find_usages`, `find_implementors`, `find_dependencies`, `find_instantiations`) continue to pass their existing tests — the use cases they consume (`FindUsagesUseCase`, `FindImplementorsUseCase`, `FindDependenciesUseCase`, `FindInstantiationsUseCase`) are untouched."
    - "Full `dotnet test ILSpy.Mcp.sln` passes with exactly 168 tests (was 173: 173 - 5 deleted AnalyzeReferences tests = 168)."
  artifacts:
    - path: "Transport/Mcp/Tools/AnalyzeReferencesTool.cs"
      provides: "DELETED — file no longer exists."
      contains: "(file absent)"
    - path: "Tests/Tools/AnalyzeReferencesToolTests.cs"
      provides: "DELETED — file no longer exists."
      contains: "(file absent)"
    - path: "Program.cs"
      provides: "DI registration list without `AddScoped<AnalyzeReferencesTool>()` at line 188."
      contains: "services.AddScoped<FindInstantiationsTool>()"
    - path: "Tests/Fixtures/ToolTestFixture.cs"
      provides: "DI registration list without `AddScoped<AnalyzeReferencesTool>()` at line 87."
      contains: "services.AddScoped<FindInstantiationsTool>()"
  key_links:
    - from: "Transport/Mcp/Tools/FindUsagesTool.cs"
      to: "Application/UseCases/FindUsagesUseCase.cs"
      via: "constructor DI — use case must still resolve after AnalyzeReferencesTool is gone"
      pattern: "FindUsagesUseCase"
    - from: "Transport/Mcp/Tools/FindImplementorsTool.cs"
      to: "Application/UseCases/FindImplementorsUseCase.cs"
      via: "constructor DI"
      pattern: "FindImplementorsUseCase"
    - from: "Transport/Mcp/Tools/FindDependenciesTool.cs"
      to: "Application/UseCases/FindDependenciesUseCase.cs"
      via: "constructor DI"
      pattern: "FindDependenciesUseCase"
    - from: "Transport/Mcp/Tools/FindInstantiationsTool.cs"
      to: "Application/UseCases/FindInstantiationsUseCase.cs"
      via: "constructor DI"
      pattern: "FindInstantiationsUseCase"
---

<objective>
Deliver CLEAN-01: hard-delete the `analyze_references` dispatcher tool. No
deprecation alias. The four individual `find_*` tools become the sole
cross-reference entry points. Tool count drops from 28 → 27.

Purpose: The 260410 audit identified `analyze_references` as an exemplar of the
dispatcher anti-pattern called out in Principle 7 of the mcp-tool-design skill.
It routes to one of four `find_*` use cases based on a string `analysisType`
parameter — hiding the option set and advertising overlap with the individual
tools. CONTEXT.md locks the decision: hard delete, no alias.

Output:
- DELETED `Transport/Mcp/Tools/AnalyzeReferencesTool.cs`
- DELETED `Tests/Tools/AnalyzeReferencesToolTests.cs`
- UPDATED `Program.cs` — remove line 188 (`services.AddScoped<AnalyzeReferencesTool>()`)
- UPDATED `Tests/Fixtures/ToolTestFixture.cs` — remove line 87 (`services.AddScoped<AnalyzeReferencesTool>()`)

The four use case classes (`FindUsagesUseCase`, `FindImplementorsUseCase`,
`FindDependenciesUseCase`, `FindInstantiationsUseCase`) and their individual
tool wrappers (`FindUsagesTool`, etc.) are UNTOUCHED. They are consumed
directly by the individual `find_*` tools, not by the dispatcher.

This plan runs in parallel with plan 09-01 (PAGINATION.md + SKILL.md edits) in
Wave 1 — the two plans touch completely disjoint files.

## Discovered deviation: second DI registration list

Research phase 09-RESEARCH.md Pitfall 1 surfaced a discovered deviation that
was NOT in CONTEXT.md: `Tests/Fixtures/ToolTestFixture.cs` (lines 73-100) is a
near-line-for-line duplicate of `Program.cs` (lines 174-201). Both register
every MCP tool in parallel `AddScoped<...>()` lists. Deleting only the
`Program.cs` line would leave the test fixture with a dangling registration
that fails to resolve, or a resolve-time NullReferenceException.

CONTEXT.md mentioned `Program.cs` under "Integration Points" but did not
mention `ToolTestFixture.cs`. This plan files-modified field includes BOTH
to make the lockstep update explicit. Both deletions must land in the same
commit to keep the build green.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-CONTEXT.md
@.planning/phases/09-pagination-contract-structural-cleanup/09-RESEARCH.md

<existing_code>
<!-- The files to delete and the exact DI lines to remove. Executor should
     verify line numbers match reality before editing — line-number drift is
     possible if other recent commits shifted the file. Grep patterns below
     are the authoritative locators. -->

Transport/Mcp/Tools/AnalyzeReferencesTool.cs (95 lines — DELETE ENTIRELY):

```csharp
// Class declaration, to confirm the file is the right one:
[McpServerToolType]
public sealed class AnalyzeReferencesTool
{
    // Constructor injects four use cases — all four stay alive because the
    // individual find_* tools consume them directly.
    public AnalyzeReferencesTool(
        FindUsagesUseCase usagesUseCase,
        FindImplementorsUseCase implementorsUseCase,
        FindDependenciesUseCase dependenciesUseCase,
        FindInstantiationsUseCase instantiationsUseCase,
        ILogger<AnalyzeReferencesTool> logger) { ... }

    [McpServerTool(Name = "analyze_references")]
    [Description("Unified cross-reference analysis tool. ...")]
    public async Task<string> ExecuteAsync(
        string assemblyPath, string analysisType, string typeName,
        string? memberName = null, CancellationToken cancellationToken = default)
    { /* switch on analysisType, delegate to one of four use cases */ }
}
```

Tests/Tools/AnalyzeReferencesToolTests.cs (DELETE ENTIRELY — contains 5 xUnit
facts, all testing the dispatcher routing):

```csharp
[Collection("ToolTests")]
public class AnalyzeReferencesToolTests
{
    public AnalyzeReferencesToolTests(ToolTestFixture fixture) => ...;

    [Fact] public async Task AnalyzeReferences_Implementors_RoutesCorrectly() { ... }
    // (4 more facts covering usages, dependencies, instantiations, invalid type)
}
```

Program.cs line 188 (the EXACT line to remove — nothing above or below):

```csharp
        services.AddScoped<FindInstantiationsTool>();
        services.AddScoped<AnalyzeReferencesTool>();   // <-- DELETE THIS LINE (188)
        services.AddScoped<GetAssemblyMetadataTool>();
```

Tests/Fixtures/ToolTestFixture.cs line 87 (the EXACT line to remove):

```csharp
        services.AddScoped<FindInstantiationsTool>();
        services.AddScoped<AnalyzeReferencesTool>();   // <-- DELETE THIS LINE (87)
        services.AddScoped<GetAssemblyMetadataTool>();
```

Neither file has any OTHER references to `AnalyzeReferencesTool` — the
constructors are fully scoped by DI, no hand-wiring anywhere else in the tree.
</existing_code>

<dependencies_to_preserve>
The deleted tool's constructor injects four use case classes. ALL FOUR MUST
REMAIN REGISTERED AND UNTOUCHED:

- `FindUsagesUseCase` — consumed by `FindUsagesTool` (existing, untouched).
- `FindImplementorsUseCase` — consumed by `FindImplementorsTool` (existing, untouched).
- `FindDependenciesUseCase` — consumed by `FindDependenciesTool` (existing, untouched).
- `FindInstantiationsUseCase` — consumed by `FindInstantiationsTool` (existing, untouched).

Executor sanity check after delete: grep the four use case type names across
the repo and confirm each still has at least one consumer. Expected counts
AFTER delete (from a quick inspection of the current tree):
- `FindUsagesUseCase`: referenced by Program.cs + ToolTestFixture.cs DI +
  `FindUsagesTool.cs` (constructor) + tests. Roughly 4-6 references.
- Same shape for the other three.

If any of these four use cases drops to zero references after the delete,
something has gone wrong — STOP and investigate. The most likely cause is
that the executor accidentally also deleted one of the individual find_*
tools' DI registration.
</dependencies_to_preserve>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AnalyzeReferencesTool source file and its DI registrations</name>
  <files>Transport/Mcp/Tools/AnalyzeReferencesTool.cs, Program.cs, Tests/Fixtures/ToolTestFixture.cs</files>
  <action>
Atomic deletion across three files:

1. **Delete `Transport/Mcp/Tools/AnalyzeReferencesTool.cs` entirely.** The file
   is 95 lines. Remove the whole file from disk (do not leave an empty stub).
   Use the standard project delete workflow (git rm, or File System delete
   followed by git add).

2. **Edit `Program.cs`** — remove exactly ONE line. The current file has at
   approximately line 188:
   ```csharp
       services.AddScoped<AnalyzeReferencesTool>();
   ```
   Remove this line. Do NOT adjust indentation on adjacent lines. Do NOT touch
   any of the other `AddScoped<*Tool>()` or `AddScoped<*UseCase>()` lines —
   every other registration stays exactly as it was.

   Line numbers may have drifted slightly since 09-RESEARCH.md was written
   (e.g., if 09-01 landed a commit that changed nothing in Program.cs, the
   line is still 188; otherwise re-locate via grep:
   `grep -n "AnalyzeReferencesTool" Program.cs`).

3. **Edit `Tests/Fixtures/ToolTestFixture.cs`** — remove exactly ONE line. The
   current file has at approximately line 87:
   ```csharp
           services.AddScoped<AnalyzeReferencesTool>();
   ```
   Same care as Program.cs: remove the line, do not adjust adjacent lines, do
   not touch other registrations. Re-locate via grep if needed:
   `grep -n "AnalyzeReferencesTool" Tests/Fixtures/ToolTestFixture.cs`.

**Important — both DI edits in the SAME commit as the file deletion.** If
Program.cs is edited but ToolTestFixture.cs is not, the test project builds
fine but fails at runtime when a test scope tries to resolve
`AnalyzeReferencesTool` (which no longer exists as a type) — the C# compiler
will actually catch this because the type reference in ToolTestFixture.cs
becomes unresolved. Either both edits land or the build stays broken.

After the edits, run `dotnet build ILSpy.Mcp.sln` to prove the tree still
compiles. Expected: 0 errors, 0 warnings (or whatever warning baseline Phase 8
landed). If there is an "AnalyzeReferencesTool could not be found" error,
there is still a dangling reference — `grep -r "AnalyzeReferencesTool" .` to
locate it.
  </action>
  <verify>
    <automated>! test -f Transport/Mcp/Tools/AnalyzeReferencesTool.cs && ! grep -q "AnalyzeReferencesTool" Program.cs && ! grep -q "AnalyzeReferencesTool" Tests/Fixtures/ToolTestFixture.cs && dotnet build ILSpy.Mcp.sln --nologo -v minimal</automated>
  </verify>
  <done>
- `Transport/Mcp/Tools/AnalyzeReferencesTool.cs` does not exist.
- `Program.cs` has no references to `AnalyzeReferencesTool`.
- `Tests/Fixtures/ToolTestFixture.cs` has no references to `AnalyzeReferencesTool`.
- `dotnet build ILSpy.Mcp.sln` succeeds (exit 0).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete AnalyzeReferencesToolTests and run full test suite</name>
  <files>Tests/Tools/AnalyzeReferencesToolTests.cs</files>
  <action>
1. **Delete `Tests/Tools/AnalyzeReferencesToolTests.cs` entirely.** The file
   contains 5 xUnit `[Fact]` methods:
   - `AnalyzeReferences_Implementors_RoutesCorrectly`
   - `AnalyzeReferences_Usages_RoutesCorrectly` (or similar)
   - `AnalyzeReferences_Dependencies_RoutesCorrectly`
   - `AnalyzeReferences_Instantiations_RoutesCorrectly`
   - `AnalyzeReferences_InvalidType_ThrowsError` (or similar)

   All 5 test the dispatcher behavior and have no value after the tool is
   deleted. Remove the whole file.

2. **Run the full test suite** to confirm the baseline is 168 tests passing
   (was 173 from Phase 8 baseline; minus 5 deleted = 168):
   ```
   dotnet test ILSpy.Mcp.sln --nologo -v minimal
   ```
   Expected result: `Passed: 168, Failed: 0, Skipped: 0`.

   If the number is NOT 168:
   - If greater than 168 (e.g., 173 still): the delete did not take effect;
     re-verify Task 1 completed and this task's deletion.
   - If less than 168 (e.g., 166 with 2 failures): one of the remaining tests
     is broken by the delete. Most likely cause: a test elsewhere that was
     asserting on the OLD tool count (e.g., "tool count is 28") — search
     `grep -rn "28" Tests/` for any such assertion and update to 27 (note:
     this is a transitional state until Plan 09-03 renames `decompile_namespace`;
     if an assertion uses `27` it may transiently fail again during 09-03
     and recover during 09-04). Another likely cause: a test in another file
     also referenced `AnalyzeReferencesTool` as a using or constructor
     parameter (unlikely but grep to confirm).

3. **Run the final grep guardrail** to confirm no dangling references anywhere
   in the source tree, test tree, or planning infrastructure:
   ```
   grep -rn "AnalyzeReferencesTool\|analyze_references" Transport/ Application/ Tests/ Domain/ Infrastructure/ Program.cs 2>/dev/null
   ```
   Expected: zero output (exit code 1 is fine; the `!` in the verify command
   below flips it).

   Note: This grep intentionally EXCLUDES `README.md` (plan 09-04 handles the
   README tool catalog), `.planning/` (history references are legitimate), and
   `.claude/skills/mcp-tool-design/SKILL.md` line 93 (historical audit
   reference in Principle 7 — RESEARCH.md confirms this is an intentional
   leave-behind).
  </action>
  <verify>
    <automated>! test -f Tests/Tools/AnalyzeReferencesToolTests.cs && ! grep -rq "AnalyzeReferencesTool\|analyze_references" Transport/ Application/ Tests/ Domain/ Infrastructure/ Program.cs && dotnet test ILSpy.Mcp.sln --nologo -v minimal 2>&1 | grep -qE "Passed:\s*168,\s*Failed:\s*0"</automated>
  </verify>
  <done>
- `Tests/Tools/AnalyzeReferencesToolTests.cs` does not exist.
- `grep -rn` in source/test/domain/infrastructure/Program.cs for
  `AnalyzeReferencesTool` or `analyze_references` returns zero hits.
- `dotnet test ILSpy.Mcp.sln` reports exactly `Passed: 168, Failed: 0`.
- SKILL.md line ~93 still mentions `analyze_references` as a historical audit
  reference — this is intentional per RESEARCH.md and not an error.
  </done>
</task>

</tasks>

<verification>
Plan-level success gate:

1. Files deleted:
   - `! test -f Transport/Mcp/Tools/AnalyzeReferencesTool.cs`
   - `! test -f Tests/Tools/AnalyzeReferencesToolTests.cs`
2. DI registrations removed from both files:
   - `! grep -q "AnalyzeReferencesTool" Program.cs`
   - `! grep -q "AnalyzeReferencesTool" Tests/Fixtures/ToolTestFixture.cs`
3. Tree builds clean: `dotnet build ILSpy.Mcp.sln` exit 0.
4. Test suite passes at the new 168-test baseline: `dotnet test ILSpy.Mcp.sln`
   reports `Passed: 168, Failed: 0`.
5. Guardrail grep across Transport/, Application/, Tests/, Domain/,
   Infrastructure/, and Program.cs returns zero hits for either token.
6. The four `FindUsagesUseCase` / `FindImplementorsUseCase` /
   `FindDependenciesUseCase` / `FindInstantiationsUseCase` are still
   registered and still consumed by their individual tools (verified
   indirectly: if they weren't, the test suite wouldn't pass 168 tests —
   the individual find_* tool tests would fail to resolve).

Post-plan state of Program.cs lines 174-201 (the tool AddScoped block) should
be ONE SHORTER than before: 28 lines → 27 lines. Same for ToolTestFixture.cs
lines 73-100.
</verification>

<success_criteria>
- `AnalyzeReferencesTool.cs` and its test file are gone from disk.
- Both DI registration lists (Program.cs + ToolTestFixture.cs) have one
  fewer entry.
- `dotnet test` baseline is 168 (down from 173).
- No test or source file anywhere in the live code references the dispatcher.
- The four underlying use cases remain registered and consumed by the
  individual `find_*` tools — no collateral damage.
- This plan runs in parallel with 09-01 (docs) because the two plans touch
  disjoint files. Plan 09-03 (CLEAN-02 rename) depends on this plan because
  both edit `Program.cs` and `Tests/Fixtures/ToolTestFixture.cs` — they
  cannot run concurrently without stomping on each other.
</success_criteria>

<output>
After completion, create `.planning/phases/09-pagination-contract-structural-cleanup/09-02-SUMMARY.md`
recording:
- `requirements-completed: [CLEAN-01]`
- Files deleted (exactly 2)
- Files modified (exactly 2)
- New test baseline: 168 (was 173)
- Surfaced deviation: `Tests/Fixtures/ToolTestFixture.cs` DI list update was
  NOT in CONTEXT.md Integration Points; added here per 09-RESEARCH.md
  Pitfall 1.
- Confirm: `FindUsagesUseCase`, `FindImplementorsUseCase`,
  `FindDependenciesUseCase`, `FindInstantiationsUseCase` are untouched and
  still consumed by the individual find_* tools.
</output>
