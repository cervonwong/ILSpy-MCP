---
phase: 15-v1.2.0-audit-iteration-2-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - CLEAN-03
gap_closure: true

must_haves:
  truths:
    - "README.md competitor comparison table row at line ~1501 reads '| **Tools** | 27 |' (not 28)"
    - "No other '28' tool-count references exist in README.md that refer to this project's tool surface"
    - "README.md:58 canonical '27 tools' statement is preserved unchanged"
  artifacts:
    - path: "README.md"
      provides: "Accurate 27-tool count in competitor comparison table"
      contains: "| **Tools** | 27 |"
  key_links:
    - from: "README.md competitor comparison table"
      to: "Runtime MCP tool surface (27 [McpServerToolType] files)"
      via: "static documentation matching runtime registration count"
      pattern: "\\| \\*\\*Tools\\*\\* \\| 27 \\|"
---

<objective>
Single-line README fix: the competitor comparison table row at `README.md:1501` currently reads `| **Tools** | 28 | ~10 | ~5 | ~3 | ~3 |` but the runtime tool surface is 27 (Phase 14 deleted `AnalyzeReferencesTool.cs`; `README.md:58` already correctly states "27 tools"). Change the stale `28` to `27`.

Purpose: Iteration-2 audit identified this row as the sole remaining CLEAN-03 doc drift. Phase 14 verification grep-checked `README.md` for "27 tools" and passed on line 58, missing the competitor table at line 1501.
Output: README competitor comparison table reflects the actual 27-tool surface; grep-verifiable; no other content touched.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2.0-MILESTONE-AUDIT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct competitor comparison table tool count from 28 to 27</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 1490-1520 (the "Comparison with other .NET decompilation MCP servers" section; the row to change is at line ~1501)
  </read_first>
  <action>
Change the single table row in `README.md` at line ~1501.

**Before:**
```
| **Tools** | 28 | ~10 | ~5 | ~3 | ~3 |
```

**After:**
```
| **Tools** | 27 | ~10 | ~5 | ~3 | ~3 |
```

Use a grep to locate the exact line first (line number may drift by one or two if the file has been edited since the audit — target by content, not line number):

```bash
grep -n "^| \*\*Tools\*\* | 28 |" README.md
```

Edit only that single matched line. Do NOT touch:
  - The surrounding table rows (Engine, Runtime required, Install options, Transport, Cross-platform, Type decompilation, etc.)
  - The "27 tools" phrasing at `README.md:58` (already correct — preserve it)
  - Any other numbers in the table (`~10`, `~5`, `~3`, `~3` refer to competitor projects and are not in scope)
  - Any other section of the README

After the edit, confirm:
1. `grep -n "^| \*\*Tools\*\* | 27 |" README.md` finds exactly 1 match
2. `grep -n "^| \*\*Tools\*\* | 28 |" README.md` finds zero matches
3. `grep -cn "27 tools" README.md` still finds the existing canonical statement (the file should contain the `README.md:58` "27 tools" phrase unchanged)

No build or test run is required — this is a documentation-only edit with no runtime impact.
  </action>
  <verify>
    <automated>cd C:/Users/cervo/Desktop/Code/ILSpy-MCP && grep -n "^| \*\*Tools\*\* | 27 |" README.md && ! grep -n "^| \*\*Tools\*\* | 28 |" README.md && grep -n "27 tools" README.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^| \*\*Tools\*\* | 27 |" README.md` finds exactly 1 match (the competitor table row, formerly `28`)
    - `grep -n "^| \*\*Tools\*\* | 28 |" README.md` finds zero matches (verified by the `!` in the automated command; exit-code 0 required)
    - `grep -n "27 tools" README.md` still finds the canonical README:58 statement (at least 1 match retained)
    - No other line in README.md changed (diff shows exactly one line modified)
  </acceptance_criteria>
  <done>Competitor comparison table row reflects 27 tools; no stale `28` remains in README; canonical `27 tools` statement at README:58 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries involved. This is a static-documentation edit to `README.md` — no code path, no data flow, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | Documentation correction | accept | Correcting a public-facing count from 28→27 reveals no new sensitive information; the count matches the runtime-observable tool surface agents already see via `tools/list`. |
</threat_model>

<verification>
1. `grep -n "^| \*\*Tools\*\* | 27 |"` README.md finds exactly 1 match
2. `grep -n "^| \*\*Tools\*\* | 28 |"` README.md finds zero matches
3. `grep -n "27 tools"` README.md still finds the canonical line 58 statement
</verification>

<success_criteria>
ROADMAP Phase 15 Success Criteria #4: `README.md:1501` competitor comparison table row reads `| **Tools** | 27 |` (not 28) — grep-verifiable ✓
</success_criteria>

<output>
After completion, create `.planning/phases/15-v1.2.0-audit-iteration-2-gap-closure/15-02-SUMMARY.md`
</output>
</content>
</invoke>