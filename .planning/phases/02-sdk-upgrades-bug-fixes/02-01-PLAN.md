---
phase: 02-sdk-upgrades-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ILSpy.Mcp.csproj
  - Tests/ILSpy.Mcp.Tests.csproj
autonomous: true
requirements:
  - SDK-01
  - SDK-02

must_haves:
  truths:
    - "ICSharpCode.Decompiler 10.0.0.8330 is the active package version"
    - "All 31 baseline regression tests pass after upgrade"
    - "MCP SDK 1.2.0 validation confirmed (already upgraded, tests green)"
  artifacts:
    - path: "ILSpy.Mcp.csproj"
      provides: "Updated decompiler package reference"
      contains: "ICSharpCode.Decompiler\" Version=\"10.0.0.8330\""
  key_links:
    - from: "ILSpy.Mcp.csproj"
      to: "Infrastructure/Decompiler/ILSpyDecompilerService.cs"
      via: "Package reference consumed by decompiler service"
      pattern: "ICSharpCode\\.Decompiler"
---

<objective>
Upgrade ICSharpCode.Decompiler from 9.1.0.7988 to 10.0.0.8330 and validate the MCP SDK 1.2.0 upgrade (already done in quick task 260407-hz7) by running all baseline tests.

Purpose: Establish the upgraded dependency foundation before fixing bugs. Per D-01 (big bang upgrade) and D-04 (MCP SDK validation).
Output: Both csproj files reference current package versions, all 31 baseline tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sdk-upgrades-bug-fixes/02-CONTEXT.md
@.planning/phases/02-sdk-upgrades-bug-fixes/02-RESEARCH.md

<interfaces>
<!-- Current package versions to change -->
From ILSpy.Mcp.csproj:
```xml
<PackageReference Include="ICSharpCode.Decompiler" Version="9.1.0.7988" />
<PackageReference Include="ModelContextProtocol" Version="1.2.0" />
<PackageReference Include="ModelContextProtocol.AspNetCore" Version="1.2.0" />
```

From Tests/ILSpy.Mcp.Tests.csproj:
```xml
<PackageReference Include="FluentAssertions" Version="8.8.0" />
<PackageReference Include="xunit" Version="2.9.2" />
```

<!-- Key APIs from ILSpyDecompilerService.cs that could be affected -->
From Infrastructure/Decompiler/ILSpyDecompilerService.cs:
```csharp
// Line 357 - constructor exclusion (relevant for SDK-05 in later plan)
Methods = type.Methods.Where(m => !m.IsConstructor).Select(MapToMethodInfo).ToList(),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify removed API non-usage and upgrade decompiler package</name>
  <read_first>
    - ILSpy.Mcp.csproj (current package versions)
    - Tests/ILSpy.Mcp.Tests.csproj (test project package versions)
    - CLAUDE.md (migration guide for ICSharpCode.Decompiler 9.1 to 10.0 breaking changes)
  </read_first>
  <files>ILSpy.Mcp.csproj, Tests/ILSpy.Mcp.Tests.csproj</files>
  <action>
    Per D-02, first grep the entire source tree to confirm zero usage of removed APIs:
    - `ITypeReference` (removed in 10.0)
    - `ToTypeReference` (removed in 10.0)
    - `UnresolvedUsingScope` (removed in 10.0)
    - `ResolvedUsingScope` (renamed to `UsingScope` in 10.0)

    If any hits found, fix them before proceeding. Research confirmed zero hits but verify.

    Then per D-01, perform big bang upgrade:

    1. In `ILSpy.Mcp.csproj`, change:
       ```xml
       <PackageReference Include="ICSharpCode.Decompiler" Version="10.0.0.8330" />
       ```
       (was 9.1.0.7988)

    2. Optionally in `Tests/ILSpy.Mcp.Tests.csproj`, bump minor versions:
       ```xml
       <PackageReference Include="FluentAssertions" Version="8.9.0" />
       <PackageReference Include="xunit" Version="2.9.3" />
       ```
       (non-breaking minor bumps per research)

    3. Run `dotnet build` to verify compilation. If any compilation errors from API changes, fix them. Expected: zero errors based on research confirming no usage of removed APIs.

    4. Run `dotnet test Tests/ILSpy.Mcp.Tests.csproj -v minimal` to verify all 31 baseline tests pass. Per Pitfall 1 from research: existing tests use `.Should().Contain()` (substring checks), so minor formatting changes in decompiler output should not cause failures.

    5. If any tests fail, investigate whether it is a genuine regression or just output format change. Fix accordingly. Do NOT add exact-match assertions.
  </action>
  <verify>
    <automated>dotnet build ILSpy.Mcp.csproj --no-restore -v quiet && dotnet test Tests/ILSpy.Mcp.Tests.csproj -v minimal</automated>
  </verify>
  <acceptance_criteria>
    - ILSpy.Mcp.csproj contains `Version="10.0.0.8330"` for ICSharpCode.Decompiler
    - ILSpy.Mcp.csproj contains `Version="1.2.0"` for ModelContextProtocol (already present, unchanged)
    - `dotnet build` exits 0 with no errors
    - `dotnet test` exits 0 with all tests passing (31+ tests)
    - No source files contain `ITypeReference`, `ToTypeReference`, `UnresolvedUsingScope`, or `ResolvedUsingScope`
  </acceptance_criteria>
  <done>ICSharpCode.Decompiler upgraded to 10.0.0.8330, project compiles, all 31 baseline tests pass confirming no regressions from either SDK upgrade.</done>
</task>

</tasks>

<verification>
- `dotnet test Tests/ILSpy.Mcp.Tests.csproj -v minimal` -- all 31+ tests pass
- `grep -r "ICSharpCode.Decompiler.*10.0.0.8330" ILSpy.Mcp.csproj` -- version confirmed
- `dotnet list package` -- shows ICSharpCode.Decompiler 10.0.0.8330, ModelContextProtocol 1.2.0
</verification>

<success_criteria>
Both SDK versions are current stable (ICSharpCode.Decompiler 10.0.0.8330, ModelContextProtocol 1.2.0), all baseline tests green, no compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sdk-upgrades-bug-fixes/02-01-SUMMARY.md`
</output>
