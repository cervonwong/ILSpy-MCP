---
phase: 01-test-infrastructure-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Tests/Tools/ListAssemblyTypesToolTests.cs
  - Tests/Tools/DecompileTypeToolTests.cs
  - Tests/Tools/DecompileMethodToolTests.cs
  - Tests/Tools/AnalyzeAssemblyToolTests.cs
  - Tests/Tools/GetTypeMembersToolTests.cs
  - Tests/Tools/FindTypeHierarchyToolTests.cs
  - Tests/Tools/SearchMembersByNameToolTests.cs
  - Tests/Tools/FindExtensionMethodsToolTests.cs
  - Tests/ToolsIntegrationTests.cs
  - Tests/TruncationTests.cs
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Every existing tool has at least 3 regression tests"
    - "Tests use deterministic assertions against known TestTargets types"
    - "Happy path tests verify structural output (headers, type names, counts)"
    - "Error tests verify McpToolException with correct error codes"
    - "Old test files are removed -- no duplicated test patterns remain"
    - "All tests pass via dotnet test"
  artifacts:
    - path: "Tests/Tools/ListAssemblyTypesToolTests.cs"
      provides: "Regression tests for list_assembly_types tool"
      contains: "ListAssemblyTypesToolTests"
    - path: "Tests/Tools/DecompileTypeToolTests.cs"
      provides: "Regression tests for decompile_type tool"
      contains: "DecompileTypeToolTests"
    - path: "Tests/Tools/DecompileMethodToolTests.cs"
      provides: "Regression tests for decompile_method tool"
      contains: "DecompileMethodToolTests"
    - path: "Tests/Tools/AnalyzeAssemblyToolTests.cs"
      provides: "Regression tests for analyze_assembly tool"
      contains: "AnalyzeAssemblyToolTests"
    - path: "Tests/Tools/GetTypeMembersToolTests.cs"
      provides: "Regression tests for get_type_members tool"
      contains: "GetTypeMembersToolTests"
    - path: "Tests/Tools/FindTypeHierarchyToolTests.cs"
      provides: "Regression tests for find_type_hierarchy tool"
      contains: "FindTypeHierarchyToolTests"
    - path: "Tests/Tools/SearchMembersByNameToolTests.cs"
      provides: "Regression tests for search_members_by_name tool"
      contains: "SearchMembersByNameToolTests"
    - path: "Tests/Tools/FindExtensionMethodsToolTests.cs"
      provides: "Regression tests for find_extension_methods tool"
      contains: "FindExtensionMethodsToolTests"
  key_links:
    - from: "Tests/Tools/*.cs"
      to: "Tests/Fixtures/ToolTestFixture.cs"
      via: "xUnit ICollectionFixture injection"
      pattern: 'Collection\("ToolTests"\)'
    - from: "Tests/Tools/*.cs"
      to: "Transport/Mcp/Tools/*.cs"
      via: "DI resolution and ExecuteAsync calls"
      pattern: "ExecuteAsync"
    - from: "Tests/Tools/*.cs"
      to: "TestTargets/"
      via: "ToolTestFixture.TestAssemblyPath"
      pattern: "_fixture.TestAssemblyPath"
---

<objective>
Write regression test classes for all 8 existing MCP tools and remove the old test files.

Purpose: Establishes the known-good regression baseline (TEST-01) that future phases depend on. Each tool gets 3-5 tests with deterministic structural assertions against the custom TestTargets assembly, replacing the unreliable runtime DLL-based tests.
Output: 8 test class files in Tests/Tools/, old test files deleted, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure-baseline/01-CONTEXT.md
@.planning/phases/01-test-infrastructure-baseline/01-RESEARCH.md
@.planning/phases/01-test-infrastructure-baseline/01-01-SUMMARY.md

@Tests/Fixtures/ToolTestFixture.cs
@Tests/Fixtures/ToolTestCollection.cs
@Tests/ToolsIntegrationTests.cs
@Tests/TruncationTests.cs

<interfaces>
<!-- From Plan 01: TestTargets types and their namespaces -->
<!-- These are the KNOWN types that test assertions will reference -->

Namespace ILSpy.Mcp.TestTargets:
  class SimpleClass { string Name; int Age; string GetGreeting(); int Calculate(int a, int b); static SimpleClass Create(string name); }
  class BaseEntity { int Id; DateTime CreatedAt; }
  class User : BaseEntity { string Email; string Username; }
  class AdminUser : User { string[] Permissions; }
  class Outer { class Inner { string Value; } enum InnerEnum { A, B, C } CreateInner(); }
  static class StringExtensions { Reverse(this string); IsPalindrome(this string); Truncate(this string, int); }
  delegate void SimpleAction();
  delegate TResult Transformer<TInput, TResult>(TInput input);
  enum DayOfWeek { Monday=1..Sunday }
  [Flags] enum Permissions { None=0, Read=1, Write=2, Execute=4, All=7 }
  [Serializable] class AttributedClass { [Obsolete] void OldMethod(); void NewMethod(); }
  class CustomInfoAttribute : Attribute { string Description; }

Namespace ILSpy.Mcp.TestTargets.Animals:
  interface IAnimal { string Name; string Speak(); int LegCount; }
  class Dog : IAnimal
  class Cat : IAnimal

Namespace ILSpy.Mcp.TestTargets.Shapes:
  abstract class Shape { abstract double Area(); abstract double Perimeter(); virtual string Describe(); }
  class Circle : Shape { double Radius; }
  class Rectangle : Shape { double Width; double Height; }
  struct Point { double X; double Y; double DistanceTo(Point); }
  struct Color { byte R; byte G; byte B; static Color Red; }

Namespace ILSpy.Mcp.TestTargets.Generics:
  class Repository<T> where T : class { List<T> Items; Add(T); T? FindById(int); IEnumerable<T> GetAll(); }
  class Pair<T1, T2> { T1 First; T2 Second; Pair<T2, T1> Swap(); }

Namespace ILSpy.Mcp.TestTargets.Services:
  class ServiceA { string Process(string); const int MaxRetries = 3; }
  class ServiceB { string DoWork(string); ServiceB Create(); }

<!-- From ToolTestFixture: how to get tools and assembly path -->
Fixture usage pattern:
  [Collection("ToolTests")]
  public class XxxToolTests
  {
      private readonly ToolTestFixture _fixture;
      public XxxToolTests(ToolTestFixture fixture) => _fixture = fixture;

      [Fact]
      public async Task TestName()
      {
          using var scope = _fixture.CreateScope();
          var tool = scope.ServiceProvider.GetRequiredService<XxxTool>();
          var result = await tool.ExecuteAsync(_fixture.TestAssemblyPath, ...);
          result.Should().Contain("...");
      }
  }

<!-- Tool ExecuteAsync signatures -->
ListAssemblyTypesTool.ExecuteAsync(string assemblyPath, string? namespaceFilter, CancellationToken)
DecompileTypeTool.ExecuteAsync(string assemblyPath, string typeName, string? query, CancellationToken)
DecompileMethodTool.ExecuteAsync(string assemblyPath, string typeName, string methodName, string? query, CancellationToken)
AnalyzeAssemblyTool.ExecuteAsync(string assemblyPath, string? query, CancellationToken)
GetTypeMembersTool.ExecuteAsync(string assemblyPath, string typeName, CancellationToken)
FindTypeHierarchyTool.ExecuteAsync(string assemblyPath, string typeName, CancellationToken)
SearchMembersByNameTool.ExecuteAsync(string assemblyPath, string searchTerm, string? memberKind, CancellationToken)
FindExtensionMethodsTool.ExecuteAsync(string assemblyPath, string targetTypeName, CancellationToken)

Error codes: "TYPE_NOT_FOUND", "METHOD_NOT_FOUND", "ASSEMBLY_LOAD_FAILED"
Error type: ILSpy.Mcp.Transport.Mcp.Errors.McpToolException (has .ErrorCode property)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write regression tests for all 8 tools (per D-05, D-06, D-07)</name>
  <files>
    Tests/Tools/ListAssemblyTypesToolTests.cs,
    Tests/Tools/DecompileTypeToolTests.cs,
    Tests/Tools/DecompileMethodToolTests.cs,
    Tests/Tools/AnalyzeAssemblyToolTests.cs,
    Tests/Tools/GetTypeMembersToolTests.cs,
    Tests/Tools/FindTypeHierarchyToolTests.cs,
    Tests/Tools/SearchMembersByNameToolTests.cs,
    Tests/Tools/FindExtensionMethodsToolTests.cs
  </files>
  <read_first>
    Tests/Fixtures/ToolTestFixture.cs,
    Tests/Fixtures/ToolTestCollection.cs,
    Tests/ToolsIntegrationTests.cs,
    Transport/Mcp/Tools/ListAssemblyTypesTool.cs,
    Transport/Mcp/Tools/DecompileTypeTool.cs,
    Transport/Mcp/Tools/DecompileMethodTool.cs,
    Transport/Mcp/Tools/AnalyzeAssemblyTool.cs,
    Transport/Mcp/Tools/GetTypeMembersTool.cs,
    Transport/Mcp/Tools/FindTypeHierarchyTool.cs,
    Transport/Mcp/Tools/SearchMembersByNameTool.cs,
    Transport/Mcp/Tools/FindExtensionMethodsTool.cs
  </read_first>
  <action>
Create 8 test class files in `Tests/Tools/`. IMPORTANT: Read each tool's ExecuteAsync implementation BEFORE writing its tests, to understand the exact output format (headers, sections, formatting). This is critical for writing structural assertions that actually match.

Every test class follows this pattern (per D-04, D-05):
- `[Collection("ToolTests")]` attribute
- Constructor injects `ToolTestFixture`
- Each test method creates its own scope via `_fixture.CreateScope()` and resolves the tool from `scope.ServiceProvider`
- Uses `_fixture.TestAssemblyPath` for the custom assembly
- FluentAssertions for all assertions
- NO conditional passes (no `if (x == null) return;` — per anti-patterns in RESEARCH.md)
- NO exception-swallowing in happy path tests

Per D-06, each tool gets 3-5 tests. Per D-07, use structural assertions on known values.

**Tests/Tools/ListAssemblyTypesToolTests.cs** (~5 tests):
1. `ListTypes_NoFilter_ReturnsAllKnownTypes` — call with null namespace filter. Assert result contains "Assembly:" header, "Types found:" with a count > 0, and known type names: "SimpleClass", "IAnimal", "Dog", "Shape", "Circle". Assert contains "class", "interface", "struct", "enum" kind keywords.
2. `ListTypes_WithNamespaceFilter_ReturnsOnlyMatchingTypes` — filter by "ILSpy.Mcp.TestTargets.Animals". Assert contains "Dog", "Cat", "IAnimal". Assert does NOT contain "SimpleClass" or "Shape".
3. `ListTypes_WithEmptyNamespace_ReturnsRootTypes` — filter by "ILSpy.Mcp.TestTargets" (exact, not sub-namespaces). Check the tool's behavior for exact vs prefix matching by reading the tool source first.
4. `ListTypes_GenericTypes_ShowsBacktickNotation` — no filter, assert result contains backtick-arity names like "Repository`1" or "Pair`2" (check tool output format first).
5. `ListTypes_InvalidAssembly_ThrowsMcpToolException` — pass `"C:\\NonExistent\\Assembly.dll"`, assert throws `McpToolException` with ErrorCode `"ASSEMBLY_LOAD_FAILED"`.

**Tests/Tools/DecompileTypeToolTests.cs** (~4 tests):
1. `DecompileType_SimpleClass_ReturnsDecompiledSource` — decompile `"ILSpy.Mcp.TestTargets.SimpleClass"`. Assert result contains "class SimpleClass", method names "GetGreeting", "Calculate", property "Name".
2. `DecompileType_InterfaceType_ReturnsDecompiledSource` — decompile `"ILSpy.Mcp.TestTargets.Animals.IAnimal"`. Assert contains "interface IAnimal", "Speak", "Name".
3. `DecompileType_NonExistentType_ThrowsTypeNotFound` — pass `"NonExistent.Type"`. Assert throws McpToolException with ErrorCode "TYPE_NOT_FOUND".
4. `DecompileType_InvalidAssembly_ThrowsAssemblyLoadFailed` — pass invalid path. Assert throws McpToolException with ErrorCode "ASSEMBLY_LOAD_FAILED".

**Tests/Tools/DecompileMethodToolTests.cs** (~4 tests):
1. `DecompileMethod_KnownMethod_ReturnsSource` — decompile `SimpleClass.GetGreeting`. Assert result contains "GetGreeting" and "Hello".
2. `DecompileMethod_CalculateMethod_ReturnsSource` — decompile `SimpleClass.Calculate`. Assert contains "Calculate" and parameter names or arithmetic.
3. `DecompileMethod_NonExistentMethod_ThrowsMethodNotFound` — pass valid type but `"NonExistentMethod"`. Assert throws McpToolException with ErrorCode "METHOD_NOT_FOUND".
4. `DecompileMethod_NonExistentType_ThrowsTypeNotFound` — pass `"NonExistent.Type"` and any method. Assert throws McpToolException with ErrorCode "TYPE_NOT_FOUND".

**Tests/Tools/AnalyzeAssemblyToolTests.cs** (~3 tests):
1. `AnalyzeAssembly_ReturnsStructuralInfo` — call with TestAssemblyPath. Assert contains "Assembly:" header, "Total Types:" with a number, "Namespaces:" section.
2. `AnalyzeAssembly_ShowsNamespaces` — assert result contains known namespaces: "ILSpy.Mcp.TestTargets.Animals", "ILSpy.Mcp.TestTargets.Shapes".
3. `AnalyzeAssembly_InvalidAssembly_ThrowsException` — invalid path. Assert throws McpToolException.

**Tests/Tools/GetTypeMembersToolTests.cs** (~4 tests):
1. `GetTypeMembers_SimpleClass_ListsAllMembers` — call for SimpleClass. Assert result contains "Methods:" or member category headers, and member names: "GetGreeting", "Calculate", "Name", "Age".
2. `GetTypeMembers_Interface_ListsMembers` — call for IAnimal. Assert contains "Speak", "Name", "LegCount".
3. `GetTypeMembers_GenericType_ListsMembers` — call for `"ILSpy.Mcp.TestTargets.Generics.Repository`1"`. Assert contains "Add", "FindById", "GetAll".
4. `GetTypeMembers_NonExistentType_ThrowsTypeNotFound` — assert throws McpToolException with "TYPE_NOT_FOUND".

**Tests/Tools/FindTypeHierarchyToolTests.cs** (~4 tests):
1. `FindTypeHierarchy_AdminUser_ShowsFullChain` — call for AdminUser. Assert result contains "AdminUser", "User", "BaseEntity", and "Object" (the full inheritance chain).
2. `FindTypeHierarchy_Dog_ShowsInterfaceImplementation` — call for Dog. Assert contains "Dog", "IAnimal".
3. `FindTypeHierarchy_Circle_ShowsAbstractBase` — call for Circle. Assert contains "Circle", "Shape".
4. `FindTypeHierarchy_NonExistentType_ThrowsTypeNotFound` — assert throws McpToolException with "TYPE_NOT_FOUND".

**Tests/Tools/SearchMembersByNameToolTests.cs** (~4 tests):
1. `SearchMembers_ByMethodName_FindsMatches` — search for "Calculate". Assert result contains "Calculate" and "SimpleClass".
2. `SearchMembers_ByPropertyName_FindsMatches` — search for "Name" with memberKind "property". Assert result contains "Name".
3. `SearchMembers_NoResults_ReturnsEmptyOrMessage` — search for "ZzzNonExistentMember". Assert result either contains "0" results or an appropriate message (read tool source to determine format).
4. `SearchMembers_InvalidAssembly_ThrowsException` — invalid path. Assert throws McpToolException.

**Tests/Tools/FindExtensionMethodsToolTests.cs** (~3 tests):
1. `FindExtensionMethods_StringType_FindsExtensions` — search for `"System.String"`. Assert result contains "Reverse", "IsPalindrome", "Truncate" (the extension methods from StringExtensions).
2. `FindExtensionMethods_TypeWithNoExtensions_ReturnsEmpty` — search for `"System.Int32"` or another type with no extensions. Assert result indicates no extensions found (read tool source to determine format).
3. `FindExtensionMethods_InvalidAssembly_ThrowsException` — invalid path. Assert throws McpToolException.

IMPORTANT: Before writing each test class, READ the corresponding tool's ExecuteAsync method to understand:
- Exact output format (what headers like "Assembly:", "Type Members:", "Search results" etc. are used)
- How types are formatted (full namespace? backtick notation for generics?)
- What the error handling path looks like
- Whether `query` parameter affects output format

Adjust assertion strings to match the ACTUAL output format observed in the tool source code.
  </action>
  <verify>
    <automated>dotnet test Tests/ILSpy.Mcp.Tests.csproj --verbosity normal 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Tests/Tools/ListAssemblyTypesToolTests.cs contains `[Collection("ToolTests")]` and `class ListAssemblyTypesToolTests`
    - Tests/Tools/DecompileTypeToolTests.cs contains `[Collection("ToolTests")]` and `class DecompileTypeToolTests`
    - Tests/Tools/DecompileMethodToolTests.cs contains `[Collection("ToolTests")]` and `class DecompileMethodToolTests`
    - Tests/Tools/AnalyzeAssemblyToolTests.cs contains `[Collection("ToolTests")]` and `class AnalyzeAssemblyToolTests`
    - Tests/Tools/GetTypeMembersToolTests.cs contains `[Collection("ToolTests")]` and `class GetTypeMembersToolTests`
    - Tests/Tools/FindTypeHierarchyToolTests.cs contains `[Collection("ToolTests")]` and `class FindTypeHierarchyToolTests`
    - Tests/Tools/SearchMembersByNameToolTests.cs contains `[Collection("ToolTests")]` and `class SearchMembersByNameToolTests`
    - Tests/Tools/FindExtensionMethodsToolTests.cs contains `[Collection("ToolTests")]` and `class FindExtensionMethodsToolTests`
    - Every test file uses `_fixture.CreateScope()` (no direct ServiceProvider.GetRequiredService on the root)
    - Every test file uses `_fixture.TestAssemblyPath` (no runtime DLL paths)
    - No file contains `if (typeLine == null) return;` or similar conditional-pass patterns
    - No file contains `catch (McpToolException` in a happy-path test method
    - `dotnet test Tests/ILSpy.Mcp.Tests.csproj` exits 0 with all tests passing
    - Total test count is >= 28 (8 tools x 3-5 tests each, minus old tests)
  </acceptance_criteria>
  <done>All 8 tools have 3-5 regression tests each using deterministic assertions against the TestTargets assembly. Tests use the shared ToolTestFixture with proper scoping. No conditional passes or exception-swallowing patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old test files (per D-08)</name>
  <files>
    Tests/ToolsIntegrationTests.cs,
    Tests/TruncationTests.cs
  </files>
  <read_first>
    Tests/ToolsIntegrationTests.cs,
    Tests/TruncationTests.cs
  </read_first>
  <action>
Delete `Tests/ToolsIntegrationTests.cs` and `Tests/TruncationTests.cs` entirely (per D-08). These files contain the old test pattern that uses runtime DLLs (System.Collections.dll) with conditional passes and duplicated DI setup. They are fully replaced by the new test classes in Tests/Tools/.

Run `dotnet test Tests/ILSpy.Mcp.Tests.csproj` after deletion to confirm all new tests still pass and no compilation errors from missing references.
  </action>
  <verify>
    <automated>dotnet test Tests/ILSpy.Mcp.Tests.csproj --verbosity normal 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Tests/ToolsIntegrationTests.cs does NOT exist
    - Tests/TruncationTests.cs does NOT exist
    - `dotnet test Tests/ILSpy.Mcp.Tests.csproj` exits 0
    - No compilation errors referencing ToolsIntegrationTests or TruncationTests
  </acceptance_criteria>
  <done>Old test files removed. Only the new Tests/Tools/ and Tests/Fixtures/ structure remains. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet test Tests/ILSpy.Mcp.Tests.csproj` passes all tests (expect 28+ tests)
- `Tests/ToolsIntegrationTests.cs` and `Tests/TruncationTests.cs` no longer exist
- Each tool has its own test class in Tests/Tools/
- No test uses runtime DLLs (System.Collections.dll, System.Runtime.dll, etc.)
</verification>

<success_criteria>
- All 8 tools have dedicated test classes with 3-5 tests each
- Tests assert on known types from the TestTargets assembly (deterministic)
- Structural assertions verify headers, type names, member names, counts
- Error case tests verify McpToolException with correct error codes
- Old test files are deleted (D-08)
- Full test suite passes: `dotnet test Tests/ILSpy.Mcp.Tests.csproj` exits 0
- This constitutes the regression baseline for TEST-01
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-baseline/01-02-SUMMARY.md`
</output>
