---
phase: 07-bulk-operations-documentation
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - README.md
autonomous: false
requirements: [DOC-01]

must_haves:
  truths:
    - "README documents all 28 tools with usage examples"
    - "Tools are grouped by category: Decompilation, IL Disassembly, Type Analysis, Cross-References, Assembly Inspection, Search, Bulk Operations"
    - "Every tool has a JSON input + trimmed output example in a disclosure accordion"
    - "Installation and configuration instructions are accurate"
  artifacts:
    - path: "README.md"
      provides: "Complete tool reference with examples"
      contains: "decompile_namespace"
  key_links:
    - from: "README.md"
      to: "Transport/Mcp/Tools/"
      via: "Documents all 28 tool files"
      pattern: "28 tools"
---

<objective>
Rewrite README.md to document all 28 MCP tools with installation instructions, configuration, and categorized tool reference with JSON examples in disclosure accordions.

Purpose: Users and AI assistants need a comprehensive reference for all available tools, their parameters, and expected outputs.
Output: Complete README.md with all 28 tools documented.
</objective>

<execution_context>
@C:/Users/cervo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/cervo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bulk-operations-documentation/07-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with all 28 tools documented</name>
  <files>README.md</files>
  <action>
Read the current README.md first, then rewrite it with the following structure (per D-13, D-14, D-15, D-16):

**README Structure:**

1. **Header** -- Project name, one-line description, badge for .NET version
2. **Overview** -- What ILSpy MCP is, what it enables (2-3 sentences)
3. **Features** -- Brief bullet list: 28 tools across 7 categories
4. **Installation** -- Keep existing installation instructions (binary download, build from source, MCP client config). Update if any details changed.
5. **Configuration** -- Keep existing appsettings.json section. Update if changed.
6. **Tool Reference** -- Grouped by category (see below), each tool with description + disclosure accordion example

**Tool Categories (per D-14):**

Enumerate all 28 tool files from `Transport/Mcp/Tools/` directory. Read each tool file to extract: tool name (from `[McpServerTool(Name = "...")]`), description (from `[Description("...")]`), and parameters (from method signature). Group as:

a. **Decompilation** (3 tools):
   - `decompile_type` -- Decompile a type to C# source
   - `decompile_method` -- Decompile a specific method
   - `decompile_namespace` -- List all types in a namespace with signatures (NEW)

b. **IL Disassembly** (2 tools):
   - `disassemble_type` -- Get CIL disassembly for a type
   - `disassemble_method` -- Get CIL disassembly for a method

c. **Type Analysis** (7-8 tools):
   - `list_assembly_types` -- List all types in an assembly
   - `get_type_members` -- Get members of a type
   - `find_type_hierarchy` -- Get base types and derived types
   - `find_implementors` -- Find types implementing an interface
   - `find_extension_methods` -- Find extension methods for a type
   - `find_compiler_generated_types` -- Find compiler-generated types
   - `search_members_by_name` -- Search for members by name pattern

d. **Cross-References** (4 tools):
   - `find_usages` -- Find all usages of a member
   - `find_dependencies` -- Find outward dependencies
   - `find_instantiations` -- Find instantiation sites
   - `analyze_references` -- Combined reference analysis

e. **Assembly Inspection** (7 tools):
   - `analyze_assembly` -- Get assembly overview
   - `get_assembly_metadata` -- Get assembly metadata details
   - `get_assembly_attributes` -- Get assembly-level attributes
   - `get_type_attributes` -- Get type-level attributes
   - `get_member_attributes` -- Get member-level attributes
   - `list_embedded_resources` -- List embedded resources
   - `extract_resource` -- Extract resource content

f. **Search** (2 tools):
   - `search_strings` -- Search string literals in IL
   - `search_constants` -- Search numeric constants in IL

g. **Cross-Assembly** (2 tools):
   - `resolve_type` -- Resolve which assembly defines a type
   - `load_assembly_directory` -- Load assemblies from a directory

h. **Bulk Operations** (1 tool -- `decompile_namespace` is in Decompilation):
   - `export_project` -- Export assembly as C# project (NEW)

Note: Adjust groupings based on actual tool count. Count actual tool files in `Transport/Mcp/Tools/` and adjust. The final count should be 28 (26 existing + decompile_namespace + export_project).

**For each tool (per D-15, D-16):**

Show tool name as heading, brief description, parameter table, then a `<details>` disclosure accordion with JSON input + trimmed output example.

For examples: use realistic but generic paths and type names. Output should be realistic snippets (not full output). Trim long outputs with `...` markers.

**Do NOT include:** Architecture section (per D-13), development/contributing guide (keep if exists and is brief), internal implementation details.

**Important:** Read EVERY tool file in `Transport/Mcp/Tools/` to get accurate tool names, descriptions, and parameter lists. Do not guess -- extract from source.
  </action>
  <verify>
    <automated>dotnet build C:/Users/cervo/Desktop/Code/ILSpy-MCP/ILSpy.Mcp.sln --no-restore 2>&1 | tail -3</automated>
  </verify>
  <done>README.md contains: installation instructions, configuration section, all 28 tools documented with category groupings, each tool has parameter table + disclosure accordion with JSON example. No architecture section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of README</name>
  <files>README.md</files>
  <action>
Present the completed README.md for human review. The human should:
1. Open README.md in a Markdown preview (GitHub or VS Code preview)
2. Verify all disclosure accordions expand/collapse properly
3. Verify tool count matches actual tools (28 total: 26 existing + decompile_namespace + export_project)
4. Spot-check 3-4 tool examples for realistic input/output
5. Verify installation instructions are still accurate
6. Verify no architecture or internal implementation details leaked in
  </action>
  <verify>Human confirms README is complete and accurate</verify>
  <done>Human approves README content, formatting, and completeness</done>
</task>

</tasks>

<verification>
- README.md exists with all 28 tools documented
- Each tool has a disclosure accordion example
- Tool categories match D-14 specification
- No architecture internals exposed (D-13)
</verification>

<success_criteria>
- README documents all 28 tools with correct names and parameters
- Tools grouped into categories per D-14
- Every tool has JSON input + output example in disclosure accordion (D-15, D-16)
- Installation and configuration sections are accurate
- Human approves the README via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/07-bulk-operations-documentation/07-03-SUMMARY.md`
</output>
